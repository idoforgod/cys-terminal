# WORKER TODO

> 이 파일은 선언이 1 KiB 예산 **밖**에 있다. 아래 채움 문단은 그 경계를 만들기 위한 것이다.
> filler line for the head-budget boundary case.
> 이 파일은 선언이 1 KiB 예산 **밖**에 있다. 아래 채움 문단은 그 경계를 만들기 위한 것이다.
> filler line for the head-budget boundary case.
> 이 파일은 선언이 1 KiB 예산 **밖**에 있다. 아래 채움 문단은 그 경계를 만들기 위한 것이다.
> filler line for the head-budget boundary case.
> 이 파일은 선언이 1 KiB 예산 **밖**에 있다. 아래 채움 문단은 그 경계를 만들기 위한 것이다.
> filler line for the head-budget boundary case.
> 이 파일은 선언이 1 KiB 예산 **밖**에 있다. 아래 채움 문단은 그 경계를 만들기 위한 것이다.
> filler line for the head-budget boundary case.
> 이 파일은 선언이 1 KiB 예산 **밖**에 있다. 아래 채움 문단은 그 경계를 만들기 위한 것이다.
> filler line for the head-budget boundary case.
> 이 파일은 선언이 1 KiB 예산 **밖**에 있다. 아래 채움 문단은 그 경계를 만들기 위한 것이다.
> filler line for the head-budget boundary case.
> 이 파일은 선언이 1 KiB 예산 **밖**에 있다. 아래 채움 문단은 그 경계를 만들기 위한 것이다.
> filler line for the head-budget boundary case.
> 이 파일은 선언이 1 KiB 예산 **밖**에 있다. 아래 채움 문단은 그 경계를 만들기 위한 것이다.
> filler line for the head-budget boundary case.

<!-- javis:todo v1 owner=worker-2 scope=pack status=active -->

- [x] 근저원인 규명
- [ ] 회귀 테스트 추가
