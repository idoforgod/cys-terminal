# Windows 0.14.4 결함 6건 — 구현 리스트업 (정규화 전문)

> 저장소 `<repo>` · 브랜치 `release/0.14-quality-line` · **전량 미커밋**
> 원시 수집 317건 → 중복 제거 후 **184건**. 출처는 병합해 전부 보존했다.
> 실측 기준선(2026-07-29, 읽기만 수행): `git status` = tracked 12건(M 11 + A 1) · untracked 7건 ·
> `python3 cysjavis-pack/bin/tests/test_import_guard.py` → **86/86 PASS**, `--selftest` → **46/46 PASS**.

---

## 0. 한눈에 — 숫자 요약

| 구분 | 건수 |
|---|---:|
| **총계** | **184** |
| 미완(todo) | 129 |
| 완료(done) | 25 |
| 철회·기각(withdrawn) | 19 |
| **오너 결정 필요(decision)** | **11** |

### 섹션별 분포

| 섹션 | 총 | 미완 | 완료 | 철회 | 결정 |
|---|---:|---:|---:|---:|---:|
| §1 ★B안 `.pth` 근본 수리 | 31 | 27 | 0 | 0 | 4 |
| §2-A 가드 라인 — 이미 완료(동결 대상) | 18 | 0 | 18 | 0 | 0 |
| §2-B 가드 라인 — **동결이므로 하지 않을 것** | 28 | 27 | 0 | 0 | 1 |
| §2-C CI·게이트 배선 잔여(B안과 독립) | 10 | 10 | 0 | 0 | 0 |
| §3 커밋·릴리스 경로 | 8 | 7 | 0 | 0 | 1 |
| §4 나머지 결함 티켓 D2~D6 | 26 | 24 | 0 | 0 | 2 |
| §5 검증·게이트(§11·§12) | 13 | 6 | 7 | 0 | 0 |
| §6 미확정·열린 질문 | 12 | 9 | 0 | 0 | 3 |
| §7 제약(항구·상시 준수) | 19 | 19 | 0 | 0 | 0 |
| §8 철회·기각 기록 | 19 | 0 | 0 | 19 | 0 |

> **주의 1** — §7 제약 19건은 "구현할 것"이 아니라 **상시 준수·최종 검증 시 재확인** 항목이다.
> 스키마에 `제약` 상태가 없어 `todo` 로 계상했다. 실질 미완 작업은 **110건**이다.
> **주의 2** — 진행중 2건(§4 후속 F1 · §5 §9 위험 4종 재대조)은 `todo` 에 포함했다.
> **주의 3(2026-08-23)** — 위 숫자 요약은 **2026-07-29 스냅샷**이며 이후 개별 행의 상태 갱신을 소급 반영하지 않는다.
> 이후 완료된 행은 상태 칸에 착지 일자를 병기한다(현재 T13). 개별 행이 정본, 요약표는 그 시점 집계다.

### ★B안 채택으로 달라진 것

| 판정 | 건수 | 내용 |
|---|---:|---|
| **착수 명분이 사라짐** | **21** | 정적 분석기 심화 20건(§2-B) + A안 4차 추상해석 재설계 1건(§8 W10) |
| 동결 목록 중 **B안에서도 살아남음** | 8 | §2-B 의 FZ11·FZ14·FZ17·FZ18·FZ19·FZ20·FZ21·FZ28 — 실행층·생산 탐지·배송 검증은 `.pth` 로 대체되지 않는다 |
| **B안 때문에 새로 생김** | 31 | §1 전부 |
| **B안이 하나도 대체 못 함** | 26 | §4 D2~D6 티켓 전부 — `.pth` 와 인과가 전혀 겹치지 않는다 |

> ★**잔여 BLOCK 숫자 불일치(오너 판단 필요)** — 기획서 §18.11.4(`plan:1227`)는 "남은 BLOCK 10건"이라 적었으나,
> 3차 보고서 BLOCK 명단 12건(`round3:160-172`) 대조 시 반영 확인은 **D1-F1 · D5-1 · T6-1 3건**뿐이라 **잔여 9건**이다.
> 게다가 계획서 `:1217` 이 "D1-F2 도 닫았다"고 기록했지만 **구획6 실측으로 미해소 확인**(§2-B FZ1) — 즉 실질 잔여는 9건이고
> 그중 1건은 "닫았다고 잘못 기록된 것"이다. 동결이라 착수는 안 하되 **기록은 정정해야 한다**.

---

## 1. ★B안 — `.pth` 근본 수리 (오너가 방금 택한 길)

> **B안 정의**(`plan:1257`): "`.pth` 를 먼저 검토·채택하고, 가드는 심층방어로 **현 수준 동결**(오늘 상태로 커밋)."
> **대가**(원문): "`.pth` 는 배송·설치 경로 변경이라 별도 검증 필요. 가드 게이트의 정적 사각은 남는다."
> **실증 현황**: master 독립 실측(mac `python3 -I`)에서 `.pth` 미적용=`ModuleNotFoundError: javis_scrub` /
> 적용=**가드 0줄로 bare import 성공**(`plan:1240-1247`). **실제 Windows embeddable 에서는 아직 아무도 본 적이 없다**(`round3:340`).

### 1-A. 검토 단계 (구현 착수 전 — 이 8건이 설계를 확정한다)

| ID | 무엇을 | 주체 | 선행조건 | 상태 | 출처 |
|---|---|---|---|---|---|
| **BR1** | ★**L1 교차오염 판정** — `.pth` 는 `__file__` 이 아니라 **env 레인**을 따른다. 스크립트 폴더 미등재 조건(=Windows embeddable)에서 `CYS_PACK_DIR=lane_a` 인 채 `lane_b/bin/main.py` 를 돌리면 **형제 모듈이 lane_a 것으로 적재**됨(구획8 실측). 즉 §7:610 "부서 격리 위반" 철회근거는 **부분적으로만** 해소된다. 이 판정 전에는 BI 계열 착수 금지 권고 | master | — | 미완 | 구획8 실측(scratchpad venv 2단 실험) · 반박 대상 `plan:1246-1247` · 철회표 `plan:610` |
| **BR2** | Windows embeddable 3.12.10 + `._pth` 실기에서 `.pth` 가 실제로 처리되는지 1회 실증. mac `python3 -I` 동치 실험만 존재 | worker | Windows 러너 또는 오너 실기 | 미완 | `plan:1240-1247` · `round3:340` · `round3:437-442` |
| **BR3** | `.pth` **배치 위치 결정** — `runtime/python/`(sys.prefix) vs `runtime/python/Lib/site-packages/`. 실측: `site.getsitepackages()` 의 Windows 분기가 prefix 를 먼저 넣고 그다음 `prefix\Lib\site-packages` 를 넣어 **양쪽 다 addsitedir 대상**. prefix 배치는 pip 조작 영역 밖이라 안전 | master | BR2 | **결정필요** | 구획8 실측(`site.getsitepackages` 소스) · 대상 경로 `windows-build.yml:62` |
| **BR4** | `.pth` **파일명 결정** — `addsitedir` 은 `*.pth` 를 사전순 처리. get-pip 이 남기는 `distutils-precedence.pth` 등과의 순서·충돌 고려 | worker | BR3 | **결정필요** | `windows-build.yml:63-67`(get-pip 부트스트랩) |
| **BR5** | `.pth` 의 팩 경로 해소를 **Rust 정본과 파리티** — 4키 우선순위(`CYS_PACK_DIR`→`JAVIS_PACK_DIR`→`AITERM_PACK_DIR`→`AITERM_JARVIS_DIR`) + `~/.cys/pack` 폴백 + **빈 문자열=미설정** 처리. 축약하면 한 개념 3구현(Rust·팩 파이썬·.pth) 드리프트 | worker | — | 미완 | `src/pack.rs:69-116` · 빈문자열 회귀 박제 `src/pack.rs:80-84, 2381-2384` · 축약형 원문 `plan:1246` |
| **BR6** | `sys.argv[0]`/`__main__` 파일 기준 해소 대안 검토 — 채택 시 L1 교차오염이 사라진다(가드와 동일 의미). 단 `.pth` 실행 시점에 `sys.argv` 가 채워져 있는지, `-c`/`-m` 경로는 어떻게 되는지 미검증 | master | BR1 | **결정필요** | 구획8 · 비교 대상 `cysjavis-pack/bin/javis_event.py:26-28` |
| **BR7** | `._pth` 3줄 동결과의 **무충돌 확인** — B안은 `._pth` 를 건드리지 않고 **별개 `.pth` 파일**을 만드는 것이므로 §11-2·§18.5 와 무충돌이고, 오히려 `import site` 유지가 B안의 **전제**다. 함께: T6-2 기각의 미검증 전제(`._pth` 가 PYTHONUTF8 을 무효화하는가)도 이때 실측 | master | BR2 | 미완 | `plan:679`(§11-2) · `plan:930`(§18.5) · `plan:83` · `round3:300, 548-551` |
| **BR8** | pip/uv 와 site-packages 공유 상호작용 점검 — pane 안 `pip install`(보호기능 ①의 핵심 경로)이 같은 공간에 쓴다. 충돌·삭제 가능성 | worker | BR3 | 미완 | `windows-build.yml:63-72`(get-pip + uv 동봉) · 보호기능① `plan:148-149` |

### 1-B. 구현 단계 (8건)

| ID | 무엇을 | 주체 | 선행조건 | 상태 | 출처 |
|---|---|---|---|---|---|
| **BI1** | `.github/workflows/windows-build.yml` "동봉 런타임 준비" 스텝, get-pip 부트스트랩(`:63-67`) **직후**에 `.pth` 생성 줄 추가. `._pth`(`:62`)는 무접촉 | worker | BR3·BR5 | 미완 | `windows-build.yml:62-67` |
| **BI2** | `.github/workflows/release.yml` 동일 스텝에 **바이트 동일**한 줄 추가 | worker | BI1 | 미완 | `release.yml:271-281` |
| **BI3** | Windows 런타임 준비를 `scripts/prep-win-runtime.sh` 로 추출할지 결정. mac 은 이미 `prep-mac-runtime.sh` 로 추출돼 한 줄 호출인데 Windows 만 두 워크플로에 인라인 복제 — `.pth` 가 복제 지점을 하나 더 늘린다 | master | — | **결정필요** | `release.yml:300-305` vs `:255-294` · `windows-build.yml:40-84` |
| **BI4** | `.pth` **멱등 가드**(`if p not in sys.path`). 실측: 같은 디렉터리가 addsitedir 로 두 번 처리되면 sys.path 에 동일 항목 2개 생성 | worker | — | 미완 | 구획8 실측(scratchpad venv `zz_cys.pth`) · 가드 선례 `javis_event.py` |
| **BI5** | `.pth` **예외 안전화** — 줄이 예외를 던지면 site 가 삼키고 **매 기동마다 stderr 로 traceback**(실측: rc=0, stderr 오염). 무음 실패 vs 시끄럽게 둘지 결정 + 훅 stderr 소비 경로 영향 점검 | worker | — | 미완 | 구획8 실측 · 영향 후보 `cysjavis-pack/hooks/guard.sh:507,527` |
| **BI6** | tauri 번들이 `.pth` 를 설치본에 싣는지 **실증**. `resources` 가 `"runtime/": "runtime/"` 디렉터리 통째 매핑이라 실릴 것으로 보이나 워크플로에 pip·site-packages 존재 검증 스텝이 **0건** | worker | BI1 | 미완 | `src-tauri/tauri.windows.conf.json:9` · `src-tauri/tauri.conf.json:37` |
| **BI7** | NSIS 재설치·업데이트 시 `SetOverwrite try` 스킵 위험 점검(주석 근거 "runtime 은 버전 핀 고정이라 스킵=동일 내용"이 **내용이 바뀌는 `.pth`** 에는 안 통함) + 언인스톨 `RMDir /r runtime` 로 정상 제거됨 확인 | worker/reviewer | BI1 | 미완 | `src-tauri/nsis-hooks.nsh:71`(SetOverwrite try) · `:15` · `:105`(PREUNINSTALL) |
| **BI8** | mac/Linux 는 `.pth` 불요 — **Windows 전용 조치**임을 명시 판정·기록(파리티 목적 도입 여부 포함). mac 런타임은 python-build-standalone 이라 스크립트 폴더가 항상 `sys.path[0]` | master | — | 미완 | `scripts/prep-mac-runtime.sh:38-40` · `plan:141-143`(§2.7) |

### 1-C. 게이트·검증 단계 (7건) — ★BG1 은 착수 **전에** 처리해야 한다

| ID | 무엇을 | 주체 | 선행조건 | 상태 | 출처 |
|---|---|---|---|---|---|
| **BG1** | ★★**③ 실런타임 재현이 `.pth` 때문에 거짓 초록이 될 위험** — `runtime_probe` 가 자식에게 `PYTHONPATH` 만 제거하고 `os.environ` 전체를 상속한다. T6 레인 인터프리터에 `.pth` 가 살아 있으면 `CYS_PACK_DIR`(또는 홈 기본 `~/.cys/pack`)이 실재하는 순간 **가드를 전부 지워도 ③ 이 통과**한다. `-S` 실행이나 pack env 무효화로 격리 필요. **순서가 뒤집히면 게이트가 먼저 눈이 먼다** | worker | **BI1 착수 전** | 미완 | `test_import_guard.py:604, 621-627`(env 처리) · `:893-914`(③ 호출부) · 실행 레인 `windows-build.yml:474-479` · 원 지적 계보 `r1-adversarial-codex.md:158` |
| **BG2** | T6 **음성 대조와 `.pth` 의 충돌 점검** — T6 는 "런타임이 스크립트 폴더를 sys.path 에 넣지 않는다"를 `exit 42` 로 증명하고 `exit 0` 이면 **"전제 붕괴"로 throw** 한다. `.pth` 가 팩 bin 만 특정해 넣으면 42 유지(무해), 일반 형태면 **CI 가 의도적으로 빨간불**. 그 스텝에서 `CYS_PACK_DIR` 을 명시적으로 비우거나 무해 경로로 고정 | worker | BI1 | 미완 | `windows-build.yml:448-472`(exit 42 계약) · `:464-470`(throw) · `round3:491` |
| **BG3** | 설치본에 `.pth` 가 **실재하는지** 검증하는 스텝 신설(`Test-Path` + 내용 대조). 현재 T6 는 `._pth` 만 Test-Path 하고 `.pth` 개념이 없다 | worker | BI1·BR3 | 미완 | `windows-build.yml:436-440`(선례) |
| **BG4** | `._pth` 3줄에 `import site` 가 살아있는지 **기계 검증**. 현재 T6 는 `Get-Content` 로 **출력만** 하고 assert 가 없다. `.pth` 는 그 줄에 전적으로 의존 | worker | — | 미완 | `windows-build.yml:440-444` · 동결 근거 주석 `:61` · 사람 체크만 있는 현행 `plan:679` |
| **BG5** | `.pth` **양성 검증** — 설치본 python3 로 임시 팩(`<tmp>/bin/probe_sib.py` + 가드 없는 `probe_main.py`)을 만들고 `CYS_PACK_DIR=<tmp>` 로 bare import **성공(exit 0)** 확인. `.pth` 가 실제로 동작한다는 유일한 실증 | worker | BI1 | 미완 | 프로브 구조 재사용 `windows-build.yml:456-472` |
| **BG6** | 레인 대조 게이트 `SUBSET_LANES` 계약 갱신 — 현재 계약은 '스위트 이름 텍스트 스캔'이라 `.pth` 검증처럼 **스위트 파일이 없는 인라인 검증**은 대조 밖 | worker | BG3 | 미완 | `ci-branch.yml:88-98`(SUBSET_LANES 정의) · `:245-262`(필수 보유·조건부 검사) |
| **BG7** | `release.yml` Windows 잡에 **설치·런타임 검증 스텝이 아예 없다**(silent install→동봉 런타임 실행→T6 계열 0건). 즉 `.pth` 는 **릴리스 레인에서 한 번도 검증되지 않고 배송**된다. 검증을 릴리스 레인에도 넣을지 결정 | master | BG3 | 미완 | `release.yml:381`(Windows 직접 빌드 + CRT gate) vs `windows-build.yml:107-135` |

### 1-D. 리스크 기록·문서 갱신 (8건)

| ID | 무엇을 | 주체 | 선행조건 | 상태 | 출처 |
|---|---|---|---|---|---|
| **BD1** | ★**다운그레이드 시 `.pth` 소실 → D1 재발** — `.pth` 는 **앱 설치본(runtime) 소유**라, 구버전(.pth 없는 0.14.4 이하) 설치 시 runtime 통째 교체로 사라진다. 이때 **팩 bin 의 가드만이 D1 을 막는다** → 오너의 "가드 동결" 결정에 기술적 근거를 부여하므로 반드시 문서에 명기 | master | — | 미완 | `src-tauri/tauri.windows.conf.json:9` · 가드 실체 `javis_event.py:19-30` |
| **BD2** | ★**`pack-update` 채널로는 `.pth` 를 못 고친다** — 무중단 팩 채널은 `~/.cys/pack` 만 갱신하고 설치본 runtime 은 무접촉. 즉 `.pth` 결함은 **핫픽스 불가·설치본 재배포 필수**. 가드는 반대로 팩 채널로 수리 가능 — **두 층의 수리 채널이 다르다**는 사실을 설계 문서에 명기 | master | — | 미완 | `src/bin/cys.rs:6958` · §2.4 팩 소유권 `plan:96-101` |
| **BD3** | 훅이 인터프리터를 **PATH 로 해소**한다(`command -v python3 \|\| python \|\| py`) → 사용자가 시스템 파이썬을 앞세우면 **`.pth` 없는 인터프리터**로 팩 스크립트가 돈다. 이 경우 가드만이 방어선. 번들 절대경로 우선으로 고정할지 결정 | master | — | 미완 | `hooks/role-bootstrap.sh:28` · `hooks/save-state.sh:12` · `hooks/guard.sh:45` · `hooks/grill-gate.sh:29,36` · PATH 주입 `src/lib.rs:98-114` · 지적 원문 `round3:513-517`(부록A5) |
| **BD4** | 사용자가 `.pth` 를 지우면 **조용히 D1 재발** — `.pth` 는 설치본 파일이라 팩의 System 등급 강제 치유(`init-pack` 스윕) 대상이 **아니고**, 훅은 fail-open 이라 증상도 없다. 재생성 주체 부재를 리스크로 기록(필요 시 부팅 시 재생성 별도 티켓) | master | — | 미완 | System 등급 근거 `plan:96-101`(`src/pack.rs:577-587`) · 훅 fail-open 51곳 `plan:125` |
| **BD5** | 실패 모드 — `import site` 제거 시 **두 결함이 동시에** 난다(①pip·site-packages 붕괴 ②`.pth` 전면 무력화 → D1 재발). 현재 동결 경고 문구는 ①만 언급 → 갱신 | worker | — | 미완 | `windows-build.yml:61` · `release.yml:276`(동일 복제) · `plan:83` |
| **BD6** | ★워크플로 `._pth` 주석 **3번째 정정** — 현 주석은 "해결 주체는 각 스크립트의 sys.path 가드다"라고 **못박고 있어** B안 채택 시 그 문장이 거짓이 된다 | worker | BI1 | 미완 | 실측 `windows-build.yml:56-61` · `release.yml:271-276` · 정정 이력 `plan:511-513`(티켓B) · `plan:896`(R3) |
| **BD7** | ★§7:610 철회표 **재개봉 기록** — 행을 **삭제하지 말고** ①철회 근거가 틀렸던 부분(`import` 줄은 실행 코드) ②그럼에도 남는 한계(BR1 — env 레인 ≠ 스크립트 레인) ③오너 재개봉 결정을 병기. 함께 §18.5 재제안 금지 목록·§11 체크리스트에 `.pth` 항목 반영 | master | BR1 | 미완 | 철회표 원문 `plan:610` · 재개봉 사유 `plan:1236-1238, 1248-1250` · §18.5 `plan:930-933` · §11 `plan:677-685` |
| **BD8** | §17 원칙 준수 — ①원칙2("'근본 수리'는 결합도를 세어본 뒤에만 쓴다") 이행을 위해 **§8 형식 결합도 표를 `.pth` 기준으로 한 장 더** 작성 ②§9 치명 위험 4종 대조를 `.pth` 기준으로 **재수행**(특히 위험④의 🟢 판정 근거 절반이 "티켓B 금지선"이라 재대조 필수) ③원칙4("수정에는 잊히지 않게 하는 장치를 같이 넣는다") — `.pth` 도 회귀 장치가 필요(=BG3·BG5) | master | BR1·BR3 | 미완 | `plan:821-828`(§17) · `plan:621-635`(§8) · `plan:650-653`(§9) |

---

## 2. 가드 라인 — **현 수준 동결** (오너 결정)

> 오너 지시: "**가드는 현 수준 동결**". 즉 **제거도 확장도 하지 않는다**. 되돌리기(revert) 대상이 아니라 **동결** 대상이다.
> B안 채택 후에도 가드는 **심층방어 2선**으로 잔류한다 — 그 기술적 근거가 §1 BD1·BD2·BD3 이다.

### 2-A. 이미 작업트리에 있는 것 (18건 · 전부 완료 · 손대지 않음)

| ID | 무엇 | 실측 | 출처 |
|---|---|---|---|
| **G1** | 팩 bin **7파일 sys.path append 가드** — `javis_event.py`(:19) · `javis_wakeup.py`(:33) · `javis_memory.py`(:38) · `javis_memory_inject.py`(:126) · `javis_orchestra.py`(:202,:627) · `javis_task.py`(:466) · `javis_phoenix_win_smoke.py`(:281) | 각 **+12줄**(코드 3 + 주석 9) M 상태 | `plan:56-66`(대상표) · `plan:493` · `plan:853-859` · `git diff` |
| **G2** | 가드 **형태 계약** — `sys.path.append(_SELF_DIR)` + `if _SELF_DIR not in sys.path` 중복가드. **`insert(0)` 금지** 근거 3종(MAJ#1 발견 목적 / stdlib shadowing / 선례 `javis_report.py:33-34`) | 7/7 append 확인 | `plan:494-504` · `plan:629` · `plan:867-872` |
| **G3** | **배치 규칙 수용기준** — 가드는 첫 형제 import 보다 위. 아래면 무효 | 7/7 통과 | `plan:505` · 검증 `plan:879` |
| **G4** | 회귀 잠금 **`cysjavis-pack/bin/tests/test_import_guard.py` 신설** — ast.parse 기반 | 922줄 · `A` 스테이징 · **86/86 PASS** | `plan:506-509` · `plan:860` · `plan:957-970` · 실측 |
| **G5** | **셀프테스트 `--selftest`** — 우회 검체 + 양성 대조 자기검사. 12→30→38→**46/46 PASS**, `_SPECIMENS` 45종(우회 33 + 양성 12) | 46/46 PASS | `plan:977-978` · `plan:1155-1175, 1221-1223` · `r1-adversarial-codex.md:188-203` |
| **G6** | **완전 레인 3종 등재** — `release.yml`(2개 잡, "회귀 6종→7종") · `ci-branch.yml` · `pack-release.yml` | M 상태 | `plan:506-509, 850-852` · `git diff` |
| **G7** | **`windows-build.yml` T6 스텝 신설** — 동봉 embeddable `python3.exe` 로 스위트 1회 실행 + `._pth` 부재 throw + 음성 대조 프로브(exit 0=전제붕괴 throw / 42=정상 / 그 외=프로브 파손 throw) + pwsh 함정 규약 | `:415-480` 실재 · +77/-1 | `plan:990-997, 1031, 1071` · `r2-ci-gemini.md:28-39` · `round2-gemini.md:24-35` |
| **G8** | **`SUBSET_LANES` 부분 레인 계약 신설** — ⊆(고아 차단) + ⊇(REQUIRED=`test_import_guard` 보유). 스텝 **제목**의 파일명 토큰 제거(제목 긁어 초록 유지되던 함정) | `ci-branch.yml:68,90,92,271-279` | `plan:999-1013` |
| **G9** | 게이트 **스텝 파서 `_step_ranges()`** — `- name:` 가정 폐기, `steps:` 리스트 항목으로 경계 재산정, **경계 실패 시 fail-closed**. 음성 시험 9종 전건 기대대로 | `ci-branch.yml:218,249` | `plan:1072, 1107-1128` · `r2-ci-gemini.md:50-60` · `round2-gemini.md:41-96` |
| **G10** | **티켓B** — 두 워크플로 `._pth` 거짓 주석 정정(6줄 블록) + `★내용 3줄 동결` 금지선 명문화. `printf` 줄 **바이트 불변** | `windows-build.yml:56-61` · `release.yml:271-276` | `plan:511-514, 631-635, 896` · `git diff` |
| **G11** | **1차 적대검증 반영** — opaque 6형태 fail-closed(exec/eval/비상수 동적 import/데코레이터 등) · destroy 이벤트 · ③ 파일별 독립 자식 프로세스 격리 · REVISE 거짓FAIL 6종 해소 · main-only 제외 | — | `plan:1056-1061, 1067-1068` · `r1-adversarial-codex.md:66-69,107-109,116-159,186` |
| **G12** | **2차 적대검증 반영** — 파괴 판정 allowlist 반전 · 별칭 고정점 전파 · 헬퍼 투영 대칭화 · 동명 재정의 fail-closed · `first_call` 순서 · TYPE_CHECKING 출처 검증 · builtins 접촉 금지 · ③ rc=0만 PASS | — | `plan:1139-1142` · `round2-codex.md:95-203` |
| **G13** | **3차 적대검증 반영 2건** — ①③ 을 rc=0 프록시에서 **`sys.modules` 실측**으로 전환(D5-1·T6-1·D6-2 모듈레벨 동시 해소) ②`sys.path` **escape → opaque**(기본 처분 반전, D1-F1 8/8 우회 차단) | `test_import_guard.py:331-372, 590-598` | `plan:1194-1219` · `round3:164,172,175` |
| **G14** | 설계 결정 — 회귀 잠금을 **preflight 부트게이트가 아니라 테스트 레인에만** 둔다(릴리스 중 부팅 실패 위험 0) | — | `plan:507` · 짝 철회 `plan:611` |
| **G15** | 선행 게이트 **U1 검증 통과** — `python3.exe javis_event.py --help` → `ModuleNotFoundError: javis_scrub` 재현 + 음성 시험 | 통과 | `plan:491, 734-744, 880-881` |
| **G16** | 제약 준수 — **Rust 코드 변경 0줄**(`src/`·`src-tauri/src/`) | `git status` 0건 | `plan:865` |
| **G17** | §8 파급 확인 — 인터페이스·구조 무변경, `javis_briefing` 의 모듈 import 시 가드 멱등, 보호기능①②와 무교차(`sys.path` vs `PATH`) | — | `plan:621-629` |
| **G18** | 신규 테스트 **`git add`** (미추적이면 등재된 CI 3종이 파일 부재로 즉시 실패) | `A` 확인 | `plan:888` |

### 2-B. ★**동결이므로 하지 않을 것** (28건)

> 각 행의 "왜 지금 안 하는가"가 이 표의 핵심이다.
> ★표시 4건(FZ1·FZ11·FZ14·FZ21)은 **동결 판단을 오너가 한 번 더 봐야 하는 것**이다.

| ID | 안 하는 일 | 왜 지금 안 하는가 | 상태 | 출처 |
|---|---|---|---|---|
| **FZ1** | ★**D1-F2** — `scan()` 에 `AugAssign`·`AnnAssign` 문형 추가(P2). `setattr(sys,…)`·`sys.__dict__`·`delattr` 처리 | **무의미해짐(동결)** — 단, ★**기록 정정 필요**: 계획서 `:1217` 이 "D1-F2 를 닫는다"고 적었으나 구획6 실측으로 **미해소 확인**(`sys.path *= 0` → 미검출 / `sys.path += [_S]` → 과탐). 착수는 안 하되 "닫았다"는 문장은 틀렸다 | 미완(동결) | `round3:165, 50-51, 185-191` · 잘못된 반영 기록 `plan:1217` · 구획6 실측 |
| **FZ2** | **D1-F3** — `_derives_from_file()` 을 오염 walk 에서 **심볼릭 경로 폴딩**으로 교체(P3). SELFDIR 정확 일치만 가드 인정 | 무의미해짐(동결). 단 보고서가 "비적대적 실수로 가장 일어나기 쉬운 부류"로 지목(2단 dirname 복붙) | 미완(동결) | `round3:166, 193-197, 362` |
| **FZ3** | **D1-F4 + D1-F5 + D6-4** — `first_call` 단일점 축약 폐기 + `hi=float("inf")` 제거(P6) | 무의미해짐(동결). D1-F5·D6-4 는 부록A(:462)가 "증거가 약한 확증 건"으로 명시 — **실물 영향 0** | 미완(동결) | `round3:167, 259, 272, 78, 89-90, 462` |
| **FZ4** | **D2-1 + D2-2 + D2-3 + D2-5 + D6-5** — 헬퍼 투영을 `def` 열거에서 **스코프별 바인딩 환경 + 효과 시그니처**로(P4a/P4b) | 무의미해짐(동결). ※D2-5/D6-5 는 **실물 출하 중인 형태**(`javis_org.py:14/18` try fcntl / except msvcrt)를 과탐하고 있다 — 동결 시 이 과탐이 CI 소음으로 남는지 확인 필요. ※D2-3 은 **보고서 자체 정정**(`:280`) — 근거 재사용 금지 | 미완(동결) | `round3:168, 169, 260, 262, 273, 59-70, 201-213, 280` |
| **FZ5** | **D3-1 + D6-1** — `module_calls` 를 '모듈 로드 시 실행되는 스코프 집합'의 **고정점**으로 재정의(P4c) | 무의미해짐(동결). 실물 실재성: 모듈 최상위 Attribute 호출 315건 · 데코레이터 적용 1건(`javis_task.py:83`) | 미완(동결) | `round3:170, 173, 72-81, 215-220, 231-237` |
| **FZ6** | **D3-2** — 지연 스코프에 `Lambda`·`GeneratorExp`·comprehension 추가(P5a) | 무의미해짐(동결). `def→lambda` 한 토큰으로 FAIL→PASS 반전 | 미완(동결) | `round3:263, 76, 84` |
| **FZ7** | **D3-3** — `_static_truth` 를 `While.test`·`For.iter` 에 연결(P5b) | 무의미해짐(동결). **비용 3~5줄**로 보고서가 명시 | 미완(동결) | `round3:264, 76, 85` |
| **FZ8** | **D3-4 + P5d** — 블록 평탄화(도달불가 문·except 본문 조건부화) + 관통 원칙 "**가드는 must-execute, 철회는 may-execute**" | 무의미해짐(동결). P5d 는 개별 수정이 아니라 재작성 전체를 관통하는 단일 원칙 | 미완(동결) | `round3:265, 76, 86-87, 365` |
| **FZ9** | **D4-1 + D4-2** — 모듈을 **해석·바인딩하는** 호출은 opaque, **조회만 하는** 형태(`find_spec is not None`)는 허용(P7) + 별칭 7형태 | 무의미해짐(동결). ★구분 없이 opaque 로 잡으면 `javis_select.py:102` 가 거짓 FAIL. ★보고서 정정(`:96`): "계약 위반" 프레이밍 **재사용 금지** | 미완(동결) | `round3:266, 267, 92-99, 96, 453-458` |
| **FZ10** | **D5-2** — ③ 대상 선정을 `toplevel` 정적 근사에서 분리 | 무의미해짐(동결). FZ11 과 같은 수정(P8a)으로 동시에 닫힘 | 미완(동결) | `round3:269` |
| **FZ11** | ★**D4-3** — ③ 루프의 `not v["toplevel"] → continue` 폐기, 프로브 대상을 **배송 표면 전 .py** 로 확장(P8a) | **축소(동결)** — 단 ★보고서가 "**0단계 — 지금 당장, 비용 0, 회귀 위험 0, 가장 값싸고 효과가 크다**"로 최우선 지정. 76개 전부 오늘 rc=0 이라 확장 즉시 초록. **B안과 독립한 실행층**이라 동결 판단을 오너가 재고할 값어치 | 미완(동결) | `round3:171, 114-119, 222-227, 323, 356` · 현행 `test_import_guard.py:905` |
| **FZ12** | **D6-2 잔여** — ③ 이 아예 안 도는 7파일(compete·idempotency·memory_inject·phoenix·phoenix_win_smoke·purge_verify·task) + 정적층 폴더 정합 미검증 | 축소(동결). §5 위험 등급 **'높음'** | 미완(동결) | `round3:270, 183, 345` · `plan:1216`(모듈레벨만 닫힘) |
| **FZ13** | **D6-3** — 검사 표면을 `skills/*/scripts` 4건까지 확장 | 축소(동결). 현재 넷 다 가드 보유 — 회귀 보호만 0 | 미완(동결) | `round3:271, 344` |
| **FZ14** | ★**probe 직전 `BIN not in sys.path` 사전 단언** — 1차 BLOCK-3 4처방 중 유일 미구현 | **★B안에서 위험이 커진다**(동결 대상이 아니라 §1 BG1 로 승격) — `.pth` 가 팩 bin 을 넣으면 ③ 의 '폴더 미등재 조건' 전제가 무너지는데 감지 장치가 없다 | 미완 → **§1 BG1 로 이관** | `r1-adversarial-codex.md:158` · 현행 `test_import_guard.py:592-599` · 반영 기록 부재 `plan:1058` |
| **FZ15** | **D2-4** — `calls.append`/`module_calls.setdefault` 에 `mo`(main_only) 전달 | 무의미해짐(동결). **비용 1줄** — 보고서가 비용을 명시한 유일 항목이라 동결과 무관하게 넣을 여지 | 미완(동결) | `round3:261` · 현행 `test_import_guard.py:464-465` |
| **FZ16** | **D4-4 / P10** — `_SPECIMENS` 에 부류당 검체 2건 이상 추가 | 축소(동결·부분 착수). ★**제약**: 검체만 넣고 분석기를 안 고치면 스위트가 **영구 빨강** — 반드시 분석기 수정과 **같은 커밋**. 동결 하에서는 미반영 부류 검체를 넣으면 안 된다 | 진행중(동결) | `round3:268, 370-371` · 부분 착수 `plan:1221` |
| **FZ17** | **부록A(2)** — ③ 을 `runpy.run_path(run_name='__guardprobe__')` 에서 `[sys.executable,'-I',path]` 자식 프로세스로 교체(생산 `__main__` 재현) | **B안에서도 살아남음**. 실측: 팩 형제 import 20건 중 모듈레벨 8건(40%)만 ③ 이 실행, 함수내 12건(60%)은 **어느 실행층도 안 닿는다**. 보고서 권고: "(1) 결과와 무관하게 즉시 넣을 값어치 — 한 플래그 작업" | 미완(동결) | `round3:444, 495-499` · `plan:1270` · 현행 `test_import_guard.py:594` |
| **FZ18** | **부록A(3)** — `selftest()` 가 검체를 **실제로 실행**해 라벨 자체를 검증(3종 체크) | **B안에서도 살아남음**. 검체 45건 중 실행되는 것 0건 → 오라클이 '저자의 믿음'. **비용 5줄**. `must_fail=False` 라벨이 틀리면 그 사각이 **회귀 잠금으로 승격**된다 | 미완(동결) | `round3:446-451, 501-505` · `plan:1271` |
| **FZ19** | **부록A(4)** — 게이트 억제(suppression) 채널 신설(pragma+사유+상한) / 계약 문서의 "과탐은 시끄럽게 드러나 즉시 고쳐진다" 논증 폐기 | 축소(동결). 실측 2단계로 **소음의 가장 싼 해소법이 미검출 생산**임이 증명됨(정당한 2줄 추가→FAIL / 그 소음을 없애는 한 줄→어느 층도 안 봄) | 미완(동결) | `round3:453-458, 507-511` · `plan:1271-1272` |
| **FZ20** | **부록A(6)** — 생산 런타임 D1 탐지기 신설: (a) 훅 래퍼가 자식 stderr 의 `ModuleNotFoundError: javis_*` 감지 → exit 0 유지하되 이벤트 1회 방출 (b) `javis_preflight.py` 부팅 1회 셀프체크(설치 팩 bin 전 파일 import-only 스모크) | **B안에서도 살아남음** — `.pth` 는 경로만 고칠 뿐 **무증상성**은 손대지 않는다. D1 이 릴리스 하나를 산 원인이 바로 무증상성인데 3라운드 24건 전부가 사전 게이트만 봤다. (b)는 B안에서 **`.pth` 자체의 회귀 탐지기**로 성격이 바뀐다 | 미완(동결) | `round3:519-523` · `plan:1274` · 훅 fail-open `hooks/appbuild-gate.sh:12` · `hooks/cys-hook.sh:10` |
| **FZ21** | ★**부록A(7)** — 게이트가 **배송 산출물이 아니라 checkout 소스**를 본다. (a) 스위트 `--bin <경로>` 인자 + T6 에 설치 팩 폴더 대상 1회 추가 (b) `git ls-files` ∆ `os.listdir(BIN)` 기계 대조 (c) `pack-update` 가 구버전 `.py` 를 prune 하는지 | **★B안에서 오히려 중요해진다** — `.pth` 자체가 **배송물**이므로 "배송 산출물을 검증하는 층"이 새로 필요하다. §1 BG3 와 같은 뿌리 | 미완(동결→§1 연동) | `round3:525-529` · `plan:1272-1273` · `windows-build.yml:475-476` · `build.rs:48-60` |
| **FZ22** | **NIT 3종** — D1-F6(`_path_aliases` 스코프 무시 과탐) · D2-6(같은 뿌리를 미검출·과탐으로 **양방향 오판** = 설계 제약 기록) · D3-5(`try import / except ImportError: 가드 후 재시도` 관용구 과탐) | 무의미해짐(동결). D2-6 은 코드 수정이 아니라 **재작성 시 지켜야 할 제약**이라 기록 가치만 있다 | 미완(동결) | `round3:287, 288, 289, 55-57` |
| **FZ23** | **회귀 미고정 3종** — ①`fp_decorator_guard` 양성 대조 검체 부재 ②`spec_from_file_location` OMIT 동작 미고정 ③`sys.path.pop(0)` 과탐 계약 미고정 | 축소(동결). 셋 다 현재는 의도대로 동작하지만 **미래에 조용히 뒤집혀도 셀프테스트가 침묵**한다 | 미완(동결) | `r1-adversarial-codex.md:121,131,217-225` · `round2-codex.md:179-187` · `test_import_guard.py:64-71` |
| **FZ24** | **NIT③** — 테스트가 `assert` 에 의존해 `python -O` 실행 시 검증 소거 | 무의미해짐(동결). 현 레인은 `-O` 미사용이라 무영향으로 이월됨 | 미완(동결) | `plan:913` |
| **FZ25** | **조건부 재정의 과탐 수용 여부** — if/else 로 같은 이름 함수를 두 번 정의하되 **양쪽 분기 모두 가드**인 정상 코드가 FAIL(rc=0인데 게이트 FAIL) | **결정필요** — 보고서 원문에 없는 구획6 파생 실측. "과탐은 안전한 방향"이라는 §18.10.2 계약(`plan:1145-1149`)과 일관하므로 의도된 대가일 수 있다. 실물 팩 실재 여부 미측정 | **결정필요** | `round2-codex.md:134-145` · 구획6 실측 `v_cond_both_guard.py` |
| **FZ26** | **3차 반영 2건의 4라운드 적대 검증** — ③ 실측 전환 · escape opaque 는 **아직 검증을 받지 않았다** | 무의미해짐(동결) — 단 **가드 동결 = 이 2건이 미검증 상태로 고정된다**는 뜻이므로 **커밋 전 오너에게 고지 필요**(문서 미기재·구조상 도출) | 미완(동결) | `plan:1221-1223` · §18.10.4-1 `plan:1278-1280` |
| **FZ27** | **상위층 객체 동일성 축 fail-open** — '무엇이 sys.path 인가'가 여전히 구문 열거. 2차 교훈(allowlist 반전)이 **한 층에만** 적용됨. 3차 판정: 2차 수정 7건 중 **부류를 닫은 것 0건** | 무의미해짐(동결). 값 격자 위 별칭/동일성 판정 = A안 재설계 영역 | 미완(동결) | `plan:1189-1192` |
| **FZ28** | **preflight C60(b) 자가경로 프로브 교정** — 부트 게이트가 검사 직전 스스로 `sys.path.insert(0, BIN)` 을 넣고 프로브해 **항상 초록**. `.pth` 가 있든 없든, 가드가 있든 없든 초록 | **B안에서도 살아남음** — `.pth` 채택 후에도 **생산 환경 D1 탐지기는 여전히 0개**. FZ20(b)와 같은 뿌리 | 미완(동결) | `plan:132` · `javis_preflight.py:3426-3432` · `plan:1272-1274` |

### 2-C. CI·게이트 배선 잔여 (10건 · **B안과 독립** — `.pth` 를 택해도 그대로 남는다)

| ID | 무엇 | 왜 아직인가 | 상태 | 출처 |
|---|---|---|---|---|
| **N1** | **F7-1** — step-level `if:` 검사를 완전 레인 3개로 확장(P9c). 실증: `pack-release.yml` 배송 팩 계약 게이트 스텝에 `if: false` 한 줄 → 게이트 **exit 0** | 단순 확장하면 `release.yml:167,198` 의 `matrix.target == 'aarch64-apple-darwin'`(의도된 1회 실행)이 거짓 실패 → **계약 설계 선행 필요** | 미완 | `round3:174, 145-154, 239-244` · `plan:1265-1267` · 유일 호출 `ci-branch.yml:276` |
| **N2** | **F7-2** — job `if:` · `continue-on-error` · `on:` · matrix · `needs` · 조기 exit 무검사(현행 6개 결정자 중 1개만 봄)(P9b) | 미착수. 실증 2건(job if=False 에도 EXIT=0 / T6 continue-on-error 에도 EXIT=0) | 미완 | `round3:274, 147, 153` |
| **N3** | **F7-3** — 레인 게이트를 정규식에서 **`yaml.safe_load` 파싱**으로 교체, ImportError·파싱 실패는 exit 1(P9a) | 미착수. 사각 3형태(`"if": false` / `if : false` / 값을 다음 줄) 전부 EXIT=0이고 ③번은 **구조적으로 정규식으로 불가** | 미완 | `round3:275, 147, 152` · 현행 `ci-branch.yml:218-268` |
| **N4** | **F7-4** — `always()` / `success() \|\| failure()` **화이트리스트만** 추가. ★스텝 경계 판별 실패 시 exit 1 은 **의도된 fail-closed 설계**이므로 손대지 마라 | NIT | 미완 | `round3:290, 328-329` |
| **N5** | **T6-3** — 백스톱 300s < ③ 프로브 예산 960s 정합 + 출력 `flush=True`(파이프 8KB 버퍼에 78줄이 갇혀 절단 시 **판정 0줄**) | 미착수. **D5-3 기각(§8 W15)과 별건으로 살아 있음** — 오독 주의 | 미완 | `round3:276, 347, 368, 298` |
| **N6** | **T6-4** — T6 를 `release.yml` windows 레그에 이식하거나 T3 앞으로 이동. `windows-build.yml` 은 `feat/windows-x64-dist` push + 수동 dispatch 에서만 돌아 **태그 릴리스 setup.exe 는 T6 를 0회 통과** | 미착수. ★보고서 자체 정정(`:281`): `pack-artifacts` 잡(`release.yml:535`, ubuntu, `if:` 없음)은 무조건 스위트를 돌린다 — 태그 레인에서 빠지는 건 "동봉 인터프리터로 돈다"는 층 하나 | 미완 | `round3:277, 340, 368, 281` |
| **N7** | ★**T6 를 실제 Windows 러너에서 1회 실행하고 첫 로그 확인** — 이 mac 에 `pwsh`·`docker`·`brew` 가 없어 **구문검사조차 불가**. 실행 경로: `feat/windows-x64-dist` push 또는 `workflow_dispatch` | **그전까지 T6 는 미검증**. 리뷰어 ACCEPT 도 정적 추론이었다 | 미완 | `plan:1282-1285` · `round2-gemini.md:34` · `round3:340`("아무도 본 적이 없다") |
| **N8** | **트리거 층 사각** — 게이트는 step-level `if:` 만 본다. `windows-build.yml` 은 `on: push branches:['feat/windows-x64-dist']` + dispatch 라 **대부분 브랜치에서 0회 실행** → SUBSET_LANES 의 '실제 1회 이상 실행' 목적이 트리거 층에서 무력 | 구획6 파생 — **기획서 어디에도 기록 없음** | 미완 | `windows-build.yml:8-11` vs `ci-branch.yml:87-99` |
| **N9** | **레인 게이트 음성 시험 자동화** — 두 리뷰어 모두 워크플로에서 스크립트를 정규식 추출해 임시 복사본에 변형 주입하는 **수동 절차**로 검증했다. CI 안에 게이트 자기검증 스텝 없음 | 구획6 파생. 부록A(3)(FZ18)과 같은 계열 | 미완 | `r2-ci-gemini.md:94-101` · `round2-gemini.md:60-92` · `ci-branch.yml:72-291` |
| **N10** | **팩 배송에서 `tests/` 제외** — `build.rs` 가 `tests`·`__pycache__`·dotfile 을 제외해 PACK_ALL 생성 → `test_import_guard.py` 는 **사용자 팩에 안 실린다**. `.pth` 든 가드든 사용자 기계 자가검증 수단 0 | 미착수 | `build.rs:49-60` · `scripts/bundle-prep.sh:30-45` |

---

## 3. 커밋·릴리스 경로 (8건)

| ID | 무엇 | 주체 | 선행조건 | 상태 | 출처 |
|---|---|---|---|---|---|
| **C1** | ★**커밋 여부 오너 승인** — 승인 전 커밋 금지가 명시 제약 | 오너 | B안 커밋 범위 결정(C2) | **결정필요** | `plan:865, 919, 1281` |
| **C2** | **커밋 범위 확정 — `git status` 로 실사할 것** · ★§18.2 목록(11종)에는 `windows-build.yml` 이 빠졌고, §18.8.4 목록(3종)에는 **티켓A 가드 7파일 + pack-release.yml + release.yml 이 통째로 빠진다**. 어느 한쪽만 보면 누락. 실측 = tracked 12건 | worker | — | 미완 | `plan:849-863`(§18.2) · `plan:1027-1032`(§18.8.4) · `git status` 실측 |
| **C3** | **untracked 문서 7종 커밋 여부** — 기획서(1286줄)·런북 + 적대검증 보고서 5종(r1-adversarial-codex·r2-ci-gemini·round2-codex·round2-gemini·**round3-workflow**). §18.2 는 2종만 기재 | 오너 | C1 | 미완 | `plan:861-862` · `git status` 실측 |
| **C4** | **설치파일 빌드 전 티켓A 반영 확인** — 팩은 `build.rs` 가 바이너리에 임베드하므로 빌드 전에 소스에 들어가 있어야 재설치·다운그레이드 시 D1 이 재발하지 않는다. ★B안이면 **확인 대상이 가드가 아니라 `.pth` 생성 스텝**으로 바뀐다(둘 다 확인해야 함) | worker | C1 | 미완 | `plan:920-921` · `build.rs` |
| **C5** | **팩 재임베드 · 서명 · 무중단 팩 채널 릴리스**(§12[5]) | 미정(§10 표에 릴리스 담당 미기재) | C1 + 리뷰 통과 | 미완 | `plan:704` |
| **C6** | **제보자 회신** — D1 확정 사실 + `insert(0)`→`append` 변경 사유. ★B안 채택 시 "`.pth` 로 근본 수리" 추가 여부는 오너 판단 | 오너 | — | 미완 | `plan:922` |
| **C7** | **T6 첫 실행 로그 확인**(=§2-C N7 과 동일 작업 · 릴리스 관문으로 재게시) | 미정 | C1·push | 미완 | `plan:1282-1285` |
| **C8** | **문서 내 낡은 수치·표·절번호 정리** — ①§18.7 첫 행동이 `70/70`·`30/30` 확인 지시(실측 **86/86 · 46/46**) ②보고서 목록 4종 → **5종**(round3 누락) ③§18.8.3 표 '적대검증 전 상태' ④§18.11.5 다음 절이 `18.10.4` 로 역행 ⑤§18.10.3 '양성 대조 13종' vs 실측 12종 ⑥§16 코드 색인 줄번호 낡음(`._pth` 를 `:57`/`:271` 로 표기, 실제 `:62`/`:277`) ⑦**오너 B안 결정이 아직 문서에 미기재** ⑧§18.4 번호 중복(1,2,3,3,4,5) | worker | — | 미완 | `plan:946-950, 1015, 1171, 1276, 786-817, 919-920` · 실측 |

---

## 4. 나머지 결함 티켓 (D2~D6) — 26건

> **★A안 확정 의존 순서**(`plan:594-595, 923`): **T-D6 → T-D3 → T-D2a(R1~R4) → T-D5(R5·R6)**.
> D3 를 먼저 고쳐야 R4 의 "잔해 없이 실패"가 성립한다. **T-D2b·T-D4 는 이 사슬 밖**(병렬 가능으로 보이나 원문 명시 없음).
> **실측: Rust 코드 변경 0줄 — D2~D6 전부 미착수 확인.**

### 4-A. 우선순위 최상 (의존 사슬)

| ID | 티켓 | 대상 | 선행조건 | 상태 | 출처 |
|---|---|---|---|---|---|
| **T1** | **T-D6 (= R7)** — cwd 미지정 시 **홈으로 해소되도록 근원 수리** + `start_master` 가 `--cwd` 명시. 효과: D2(신뢰창) 발현률 100%→낮춤. 단독으로도 정합성 결함 | `state.rs` pane 생성 + `src-tauri/src/main.rs:2124-2130` | 없음 — **사슬 최선두** | 미완 | `plan:529, 592, 923, 347-368` · 실측 `main.rs:2124`(--cwd 없음) |
| **T2** | **D6 ①/② 택1** — ①호출부가 `--cwd` 명시(차선) vs ②데몬 측 cwd 기본값 근원 수리(**원문이 ②를 정도로 지목**) | 위와 동일 | — | **결정필요** | `plan:361, 367, 368` |
| **T3** | **T-D3** — `cause="reap"` + 생성 시 발급 nonce 소지자에 한해 **자기 생성 surface** close 허용. 고아 좌석의 유일한 발생원. 실측 현행: cys.rs 는 이미 `cause="reap"` 로 요청하나 `caller_sid != sid` 가드에 막힘 | `src/bin/cys.rs:5017`(실측 5019-5022) + `src/bin/cysd/handlers.rs:1646`(실측 1640-1655) | T1 다음 | 미완 | `plan:532, 299, 302, 923` |
| **T4** | **D3 수리안 3중 택1** — 1안 nonce 예외(★권장) / 2안 데몬이 롤백 수행(surface.create 에 자동 회수 계약) / 3안 익명 경로 위임(**취약·비권장**) | — | — | **결정필요** | `plan:299-302` |
| **T5** | **R1** — 신뢰창 자동확인을 **1회만** 시도. 현행 위반: `send_key Return → 2초 sleep → continue` 무한 재전송 | `src/bin/cys.rs:4648-4655` | T3 + **M1 규명** | 미완 | `plan:586, 530` · 실측 `cys.rs:4648-4656` |
| **T6** | **R2** — 전송 후 **프롬프트 소멸 확인** 뒤에만 진행 | 위와 동일 | T5 | 미완 | `plan:587, 250` |
| **T7** | **R3** — 소멸 안 하면 **사람에게 위임**. pane·UI 양쪽에 안내: `폴더 신뢰 확인이 필요합니다 — 이 창에서 직접 Enter를 눌러 주세요 (N초 대기 중…)` | 위 + UI | T5·T6 | 미완 | `plan:588` |
| **T8** | **R4** — 대기 중 claude **미살해** · 타임아웃 시 **잔해 없는 정직한 실패**. ★"잔해 없이 실패"는 T3 가 먼저 고쳐져야 성립 | 위와 동일 | **T3 필수** | 미완 | `plan:589, 595` |
| **T9** | **R6 / T-D5①** — 기동 중 `에이전트 기동 중…(최대 60초)` 표시. 백지 금지. 실측: `ui/src/main.ts` 에 해당 문자열 0건 | `ui/src/main.ts` | T5~T8 다음 | 미완 | `plan:591, 534, 923` |
| **T10** | **R5 / T-D5②** — 실패 진단을 **그 창 안에** 남긴다(토스트 1회로만 뜨면 놓치면 소실). 진단문 자체는 이미 정확 — **도달 경로만** 고친다 | `ui/src/main.ts` · 진단 원본 `cys.rs:4675-4695` | T9 와 동일 티켓 | 미완 | `plan:590, 534, 336-342` |
| **T11** | **T-D5③** — 롤백된 창의 **유령 탭 정리**(데몬엔 없는데 UI 탭 잔존). ★R1~R7 에 대응 항목이 없어 T-D5 에서만 추적된다 — **누락되기 쉬움** | `ui/src/main.ts` 또는 데몬 이벤트 | T9 | 미완 | `plan:534, 343` |

### 4-B. 사슬 밖 (병렬 가능)

| ID | 티켓 | 대상 | 상태 | 출처 |
|---|---|---|---|---|
| **T12** | **T-D2b** — 선행 신뢰 등록(온보딩/preflight). 최초 기동 전 사람이 1회 승인하도록 안내하거나, **신뢰창을 아예 띄우지 않는 실행 형태** 채택. 자동확인에 의존하지 않는 근본 회피책 | 대상 파일 미지정(`온보딩/preflight`) | 미완 | `plan:531, 251, 253` |
| **T13** | **T-D4** — `screen_tail_is_shell_prompt` 끝문자 집합에 **`>` 추가** + Windows 프롬프트 테스트 동봉. ~~실측 현행: `% $ # ❯` 4종뿐~~ → **2026-08-23 착지(U-9)**: `windows_shell_prompt_shape`(PowerShell `PS …>` · cmd `<드라이브>:\…>`)를 `windows` 축과 AND 로 추가, 순수 본체 `screen_tail_is_shell_prompt_on(text, windows)` + 진리표 테스트 동봉. Unix 4종(`% $ # ❯`)은 OS 게이트 없이 유지. ★영향 범위는 ready_marker 없는 에이전트(codex·grok)의 시간 폴백 직전 검사 **+ readiness 안전 밸브**(U-5 argv 승격으로 `agent_alive` 가 래퍼까지 세게 되자 이 술어와 AND 를 걺 · ★2026-08-23 U-13 에서 밸브 판정이 `src/readiness.rs` 의 증거 사다리 첫 항으로 이사 — 합성 함수 `readiness_safety_valve(_on)` 은 사라졌고 AND 계약은 그 자리에 그대로 있다) | `src/bin/cys.rs` `screen_tail_is_shell_prompt`/`_on`·`windows_shell_prompt_shape` (구 기재 `:4399-4407` 은 이미 무효한 행번호) | **완료**(2026-08-23) | `plan:533, 307-321` · 착지 근거 Windows 실기 캡처 `PS C:\WINDOWS\system32\WindowsPowerShell\v1.0>` |
| **T14** | **D5-a** — 기동 0~60초 **백지 구간**에 진행 상태 노출(원문은 구간 진단만·처방 없음) | UI | 미완 | `plan:339-341` |
| **T15** | **§3.4.1 계정 디렉터리 불일치 진단** — `agents.json` 이 `CLAUDE_CONFIG_DIR` 를 주입하는데 `claude-2` 는 실행파일이 아니라 **PowerShell 함수**라 자체 디렉터리를 쓸 수 있다 → 신뢰 기록이 서로 안 보임. 충돌 여부 점검 | 진단 | 미완 | `plan:259-267`(처방은 §6.5 D-티켓으로 이관) |
| **T16** | **D3 즉시 우회 안내** — 고아 좌석은 **자기 창 안에서** `cys close-surface surface:<N> --reap`. ⚠`--reap` 없이 닫으면 묘비 → 그 역할이 자동 부활에서 **영구 제외**(`governance.rs:1916-1919`), 되돌리려면 `cys tombstone <role> --remove` | 문서 | 미완(기획서 기재는 완료) | `plan:304-305, 548-549` |
| **T17** | **§6.6 우회 산물이 '반쪽 좌석'임을 사용자 대면 문서에 명시** — 원문이 명령형으로 남긴 미이행 액션("임시 방편임을 문서에 명시할 것") | 릴리스 노트/온보딩(대상 미지정) | 미완 | `plan:546, 536-544` |
| **T18** | **§6.1 절차의 Windows 유효화** — "⚠Windows 무효" 표기를 T-D6→T-D3→T-D2a 완료 시점에 걷어낸다(삭제가 아니라 **상태 갱신** — 정정 이력 보존) | 문서 | 미완 | `plan:469-471, 481-488` |
| **T19** | **A안 검증 기준 3종(수용 조건)** — ①신규 Windows 설치에서 ▶CEO 1회로 master 기동(사람 Enter 1회도 성공 인정) ②실패 시 `cys list` 에 role 쥔 고아 좌석 **0개** ③실패 원인이 **그 창 화면에** 남아 있다. 셋 다 통과해야 A안 완료 | reviewer(§10 적용) | 미완 | `plan:597-600` |

### 4-C. 증거·좌석 복구 (규명 선행)

| ID | 무엇 | 주체 | 상태 | 출처 |
|---|---|---|---|---|
| **T20** | **§13 D2 원인 규명 — 가설 H1~H5 판별 증거 수집**. H1 watchdog 중복 kill(`cys events --after-seq <N>`, `CYS_AUTOKILL_DUP`) / H2 자원 게이트(`~/.cys/state/boot-last.json`) / H3 claude 자체 종료(`CLAUDE_CONFIG_DIR` 세션 파일) / H4 외부 텍스트 주입(`cys read-screen --surface`) / H5 OS 크래시·OOM. **수집 우선순위**: scrollback → claude 로그 → 이벤트 되감기 → boot-last.json → OS 로그. ★플래그 실측 주의: 과거 재생은 `--after-seq`(`--since` 아님), `--reconnect` 는 재접속 옵션 | CSO | 미완 | `plan:713-728` |
| **T21** | **§14B 좌석 상태 확인** — `cys status --json`(master 의 role·agent·agent_alive·seat) + `cys list`. 판정: `role=master` + `agent=null` + `agent_alive=null` → **반쪽 좌석 확정** | CSO | 미완 | `plan:746-751` |
| **T22** | **오너 기계 master 좌석 수동 복구**(§6.6 우회로 · §12[0]) — 새 창 → `claude-2 --dangerously-skip-permissions` → 신뢰창에 **사람이 직접 Enter** → `cys claim-role master` → "너는 마스터다". ⚠산물은 반쪽 좌석(임시). **모든 후속 단계의 선행** — "지금 상태는 스스로 낫지 않는다" | 오너 | 미완 | `plan:536-544, 637-642, 691, 939-941` |
| **T23** | **소실된 리뷰어 2좌석 재기동** — 3라운드 적대 검증 체제 유지에 필요(CSO·worker 는 구세대로 생존). ★원문은 '2명 소실' 사실만 기재하고 재기동 지시는 명시 안 함 — 파생 항목 | 오너/master | 미완 | `plan:937` |

### 4-D. 릴리스 후 후속 F 계열 (§6.4)

| ID | 무엇 | 상태 | 출처 |
|---|---|---|---|
| **T24** | **F1 — 팩 파이썬 테스트를 Windows 잡에서도 실행**. 이 결함 클래스의 **유일한 구조적 방지책**. 34개 중 9개가 unix 전제 → 부분집합 25개 확정 + skip 마커 분류가 **선행**. 현재 진행분: `test_import_guard` 1종만 T6 에 등재 + SUBSET_LANES 신설. **나머지 24종 미반영** | 진행중 | `plan:519, 904, 706, 776` |
| **T25** | **F2 — fail-closed 지점 실패를 승인 Feed(`cys feed push`)로 승격**. 이유: 가드를 넣어도 '조용한 죽음' 구조는 그대로(훅 fail-open exit 0). ★**B안을 택해도 그대로 남는다** — `.pth` 는 경로만 고친다 | 미완 | `plan:520, 706, 777` |
| **T26** | **F3 — Windows in-seat 재연결 지원(셸 인지 렌더)**. §7 철회②의 '올바른 버전' — Windows pane 셸이 powershell 기본이고 `CYS_SHELL` 로 cmd 오버라이드 가능(`state.rs:2437-2459`)하므로 셸 문법 인지 별도 렌더 함수 + 테스트. **기존 `render_launch` 는 무변경**(계정격리 계약 보존) | 미완 | `plan:521, 609, 706, 683` |

---

## 5. 검증·게이트 (§11·§12) — 13건

### 5-1. §11 보호 대상 무손상 체크리스트 (7종)

| # | 체크 | 현재 | 출처 |
|---|---|---|---|
| **V1** | `src/lib.rs` 경로 합성 4함수 무변경 — `runtime_bin_dirs` / `runtime_prefixed_path` / `compose_pane_path` / `compose_unix_pane_path` | ✅ **완료**(`src/` 변경 0건) | `plan:678` |
| **V2** | `python312._pth` **내용 무변경**(3줄 · `import site` 유지) | ✅ **완료**(`printf` 줄 diff context) · ★B안이 `._pth` 를 건드리면 이 체크 개정 필요, **별도 `.pth` 파일이면 무충돌**(§1 BR7) | `plan:679` · 실측 `windows-build.yml:62` · `release.yml:277` |
| **V3** | 워크플로 get-pip 부트스트랩 단계 무변경 | ✅ **완료**(pip/ensurepip 추가·삭제 0줄) | `plan:680` |
| **V4** | pane 안에서 동봉 런타임 발견 — `npm -v` · `git --version` · `python3 -V` | ⬜ **미완** — Windows 실기 pane 필요(정적 검사 불가) | `plan:681` |
| **V5** | claude 사후 설치 후 **새 pane** 에서 `claude --version` 발견(레지스트리 재합성) | ⬜ **미완** — Windows 실기 필요 | `plan:682` |
| **V6** | `cys.rs:6805` 가드 + `render_launch`(`cys.rs:4477-4492`) 무변경 = 계정격리 계약 보존 | ✅ **완료** | `plan:683` |
| **V7** | `governance.rs:276-360` 사망 감지 블록 무변경 | ✅ **완료** · ⚠`role.seat_vacant`(§6 Q9)를 이번 판에 넣으면 이 체크와 충돌 | `plan:684` |

### 5-2. §12 실행 게이트 (3종)

| # | 게이트 | 현재 | 출처 |
|---|---|---|---|
| **V8** | ① **U1 미검증 상태에서 티켓A 착수 금지** | ✅ **완료**(U1 참 확정으로 해제) | `plan:709, 774, 491` |
| **V9** | ② **증거 확보 전 재현 시도 금지** | ⬜ **미완**(상시 적용 · §13 증거 수집 전제) | `plan:709` |
| **V10** | ③ **수정은 오너 승인 후** | ⬜ **미완**(상시) · ★B안 착수에도 적용되는지 명시 없음 — 오너의 B안 결정 자체를 승인으로 볼 여지 | `plan:709, 919, 1281` |

### 5-3. 검증 프로토콜 (3종)

| # | 무엇 | 현재 | 출처 |
|---|---|---|---|
| **V11** | **라운드별 고정 검증 세트** — 스위트 전건(52→70→78→**86/86**) · 셀프테스트(12→30→38→**46/46**) · 실파일 뮤턴트 **4종**(가드 제거 2 + 경로 오타 2) 전건 탐지 · 정적↔실제 대조 불일치 0 · 레인 7스위트 회귀 · `py_compile` · 워크플로 4개 YAML 파싱 · Rust·`._pth` **변경 0건** | ✅ **완료**(2026-07-29 재실행 확인) | `plan:1069, 1075-1087, 1155-1175, 1221-1223, 1278-1280` |
| **V12** | **§11 감사 5종 커밋 직전 1회 더 재수행** — 그 뒤로 코드가 3차 반영분만큼 더 변했다 | ⬜ **미완** · ★B안이면 감사 항목에 `.pth` 배치가 새 불변식으로 추가돼야 한다 | `r2-ci-gemini.md:70-84` · `round2-gemini.md:100-107` |
| **V13** | **§9 치명 위험 4종 대조** — ①폭주 ②무clear ③주기 자가치유 전멸 ④전 pane 에이전트 사망. 티켓A/B 기준 전건 🟢 | 🟡 **진행중** — ★대조 대상이 "티켓A/B"였으므로 **`.pth` 기준 재수행 필수**. 특히 위험④의 🟢 근거 절반이 "티켓B 금지선"이다(=§1 BD8) | `plan:650-655` |

### 5-4. §12 실행 단계 매핑 (참조 — 중복 계상 제외)

| 단계 | 주체 | 상태 | 대응 항목 |
|---|---|---|---|
| [0] ▶CEO 로 master 좌석 복구 (코드 변경 0) | 오너 | 미완 | §4 T22 |
| [1] 증거 수집(§13) + U1 검증(§14) — 기계 재시작 금지·읽기 위주 | CSO | 부분 완료(U1 ✅ / §13 미완) | §4 T20 · V8 |
| [2] 판정·분해·위임 — **master 직접 구현 금지** | master | ✅ 완료 | §7 X12 |
| [3] 구현(티켓A·B) | worker | ✅ 완료 | §2-A |
| [4] reviewer1 적대검증 → reviewer2 감사 | reviewer | ✅ 완료(3라운드) | §2-A G11~G13 |
| [5] 팩 재임베드·서명·릴리스 | 미정 | 미완 | §3 C5 |

---

## 6. 미확정·열린 질문 (12건) — 구현 전 선행

| ID | 질문 | 왜 막고 있나 | 상태 | 출처 |
|---|---|---|---|---|
| **Q1** | ★**M1 — 왜 `\r` 이 취소로 처리되는가**. `src/lib.rs:498-499` 가 `"Return"\|"Enter" => b"\r"`(0x0D) 정상 바이트를 보내는데 Windows claude TUI 가 취소/종료로 처리. 세 가설: (a)ConPTY 경로에서 Ink TUI 핸들러 도달 전 소실/변환 (b)입력 핸들러 부착 **이전** 도착(첫 폴링 2.5초는 이른 편) (c)2.5초 주기 반복 전송이 후속 화면에서 원치 않는 선택을 누름 | **T-D2a(R1~R4) 착수를 블로킹**. 대조 실험은 확정(사람 Enter 통과 / cys send_key 종료, 2회 재현) — **원인만 미확정** | 미확정 | `plan:246-250, 232-239, 924` |
| **Q2** | **D6-② 기전 미확정** — `state.rs` 의 `cwd.unwrap_or_else(|| dirs::home_dir()…)` 가 **왜 Windows 경로에서 적용되지 않는지**. 원문이 "적용되지 않는 것으로 **보인다**"로 서술 | T-D6 근원 수리 설계의 전제 | 미확정 | `plan:347-368, 359-360` |
| **Q3** | **§2.4 팩 파일 소유권 — 현장 수리가 원복된다**. `src/pack.rs:577-587` 단일 SOT 3등급 중 `bin/*.py`·`acl.json` 이 System(강제 치유), 회귀 테스트 `pack.rs:2286-2290` 이 박제. 처방 **미제시** | 소유권 등급 변경은 회귀 테스트 수정 동반. ★B안이면 팩 파일을 안 고쳐도 되어 회피될 가능성(원문 근거 없는 추론) | 미완 | `plan:96-101` |
| **Q4** | **§2.6 관측 침묵 구조** — 훅 `exit 0` 51곳 · `ui/src/main.ts` 빈 catch 63곳 · Rust `let _ =` 613곳 · `unwrap_or(false/default)` 290곳 · 팩 `except: pass` 132곳. 대부분 의도된 fail-open 설계이나 부작용으로 결함이 조용히 산다. 처방 **미제시** | 전면 개조는 설계 철학과 충돌 → **범위 한정 필요** | 미완 | `plan:122-131` |
| **Q5** | **§2.7 검출 구조 플랫폼 비대칭** — 팩 파이썬 테스트 34개가 전부 macOS 잡, 그중 9개는 unix 전제. 실행 파이썬 3종(개발기 3.9.6 / mac 3.12.13 / Windows 동봉 3.12.10 embeddable). 현장 결함 1·2호 모두 Windows | T6 로 1건만 Windows 실런타임 진입 — **나머지 33개 편중 그대로**. 처방 없음 | 진행중 | `plan:134-143` |
| **Q6** | **§5.1 SEAT 수리가 부활 경로 한 곳에만 적용됨** — '역할 등록'과 '좌석 점유' 구분이 사망 감지·배달 게이트·관측에는 안 옮겨졌다. 확장 여부 판단 필요 | §5.2 제약(agent_meta ≠ seat, 질문이 다르다)과 충돌 금지. ★원문은 사실 서술이고 지시가 아니다 — 구현 항목인지 배경인지 애매 | 미완 | `plan:417-418` |
| **Q7** | **§15 R1 — 원인이 Claude CLI 자체면 우리가 못 고침**. 오너 결정: **우회책을 찾아 산다**. "우회책"이 무엇인지 미정의 | §13 H3 증거 수집 결과에 종속 | 미확정 | `plan:773` |
| **Q8** | ★**§15 열린질문1 — 회귀 테스트를 팩 테스트(python)로 둘 것인가, Rust 소스 grep 테스트로 둘 것인가**. "P0-7 선례는 후자"(`src/bin/cysd/main.rs:2306-2318`) | 문서상 **미결인 채로 구현이 선행**됐다(현재 팩 python). ★B안이면 검증 대상이 "7파일에 가드가 있는가" → "`.pth` 가 배치·동작하는가"로 바뀌어 **질문을 다시 던져야** 한다 | **결정필요** | `plan:780` |
| **Q9** | **§15 열린질문2 — `role.seat_vacant` 저심각도 이벤트를 이번 판에 넣을 것인가, F 계열로 미룰 것인가**. §7 철회①의 안전한 대체("교체가 아니라 **추가**", 자동 조치 없음) | 넣으면 §11-7(governance.rs 무변경)과 충돌. 구현 위치·이벤트 스키마 미명시 | **결정필요** | `plan:608, 781` |
| **Q10** | **§15 열린질문3 — F1 Windows CI 의 테스트 분류 기준(어떤 9개를 skip 처리할 것인가)**. 9개 목록이 문서에 없다 | F1(§4 T24) 착수를 블로킹 | **결정필요** | `plan:782, 776` |
| **Q11** | **§4.2 순서 제약 (b)→(a) 의 현재 유효성** — "(b) 반쪽 좌석 수리가 (a) 원인 규명의 선행"(b는 정식 노드가 아니어서 다음에 죽어도 증거가 안 남는다) | ★D2 가 §3.4 에서 이미 확정돼 (a)의 상당 부분이 규명됐으므로, 이 순서 제약이 **여전히 유효한지 재확인 필요**(§4.2 는 D2 확정 이전 서술로 보임) | 미확정 | `plan:389-406` |
| **Q12** | **'빈 surface 잔존'(종결) vs D3 '고아 좌석'(role 을 쥔 채 에이전트 없는 좌석)의 경계** — 원문이 명시적으로 구분하지 않는다 | 수리 설계 시 **혼동 위험** — 종결된 건을 다시 여는 사고 방지 | 미확정 | `plan:10` · `plan:299-305` |

---

## 7. 제약 — 건드리면 안 되는 것 (19건 · 상시 준수)

> 이 표의 항목은 "구현할 것"이 아니라 **모든 수리가 지켜야 하는 불변식**이다. 최종 검증 시점에 훼손 여부를 재확인한다.

| ID | 제약 | 근거·앵커 | 출처 |
|---|---|---|---|
| **X1** | **보호 대상 ① `runtime_bin_dirs`** — 동봉 `runtime/{python,git/cmd,git/usr/bin,node}` 를 pane PATH **선두**에 올려 pane 안 `npm i -g`·`curl \| bash` 를 가능케 한다 | `src/lib.rs:103-108` | `plan:145-149, 928-929` |
| **X2** | **보호 대상 ② `runtime_prefixed_path`** — Windows 는 데몬이 `WM_SETTINGCHANGE` 를 못 받아 PATH 가 stale 하므로 **spawn 마다 레지스트리에서 재합성**, unix 는 `~/.local/bin` 무조건 append | `src/lib.rs:153-184` | `plan:145-149, 928-929` |
| **X3** | **`python312._pth` 내용 3줄 동결 · `import site` 삭제 금지** — 빼면 pip·site-packages 가 죽어 보호기능①(앱 내 CLI 설치) 붕괴 + **`.pth` 처리 자체가 무력화**(B안의 전제) | `windows-build.yml:62` · `release.yml:277` | `plan:82-83, 513-514, 679, 930` |
| **X4** | **append 교리 — precedence 강등 금지** — "append 인 이유는 MAJ#1과 동일: **발견이 목적** — 기존 항목의 precedence 를 강등하지 않는다." `sys.path`·`PATH` 어느 쪽이든 `insert(0,…)` 가 아니라 append | `src/lib.rs:153-184` 주석 | `plan:151-152, 494-504, 629, 867-872` |
| **X5** | **팩 97% System 등급 → 모든 수리는 저장소 소스에 반영해야 유효** — 현장(오너 기계) 로컬 수리는 `init-pack` 스윕에 원복된다 | `src/pack.rs:577-587` | `plan:96-101, 931` |
| **X6** | **seat 는 watchdog 단일 writer · 5초 주기** — `SeatState{Unknown=0,Occupied=1,Empty=2}` 는 커널 사실(자손 프로세스 유무). 초기값 Unknown. 다른 곳에서 seat 를 쓰면 SOT 가 깨진다 | `governance.rs:1365-1392, 1405-1420` · `WATCHDOG_INTERVAL_SECS:13` · `state.rs:1825` | `plan:412-416` |
| **X7** | **사망 감지 상태 기계를 함부로 건드리지 않는다** — agent_meta 필수 → `cmdline_matches_agent(bin_base)` 의미적 판정 → agent_seen 래치·exit_notified 단일통지 → auto_restart opt-in → 401/token_expired 차단+poison → 3회 서킷브레이커. **핵심: agent_meta 는 식별자가 아니라 판정 재료이며 seat(존재 판정)와 질문이 다르다 — 합치지 마라** | `governance.rs:276-360` | `plan:420-431` |
| **X8** | **배달 게이트 현행 유지** — pause / queue_paused / busy / 최근 사람 입력(3초) / SEAT 게이트 순 보류. SEAT 게이트는 `role.is_some() && seat==Empty` 에서만 보류하고 **`Unknown` 이면 배달**, **role 없는 맨 셸은 종전대로 통과** | `governance.rs:2060-2125` | `plan:433-436` |
| **X9** | **`render_launch` 출력이 회귀 테스트에 박제됨** — `render_launch_os_aware_unix_byte_identical`(`cys.rs:10467`) · `render_launch_no_env_agent_unchanged`(`:10496`) | `cys.rs:4477-4492` | `plan:452-458` |
| **X10** | **`cfg!(unix) \|\| env_injected` 는 플랫폼 차별이 아니라 계정격리 계약** — unix 는 env 가 명령줄에 실리지만 Windows 는 `surface.create` 시점에만 주입 가능. '차별'로 오해해 완화하면 **멀티계정 격리 붕괴** | `cys.rs:6805` (`:120` 동일 코드) | `plan:452-457, 609` |
| **X11** | **Windows pane 셸 문법을 하나로 가정하지 마라** — 기본 powershell.exe, `CYS_SHELL` 로 cmd.exe 오버라이드 지원. ★D4 의 `>` 판정에 직접 영향(cmd 프롬프트도 `>` 로 끝남) | `state.rs:2437-2459` | `plan:459` |
| **X12** | **역할 배치 — 구현은 worker, master 직접 구현 금지**. "7파일은 master가 '간단하니 내가' 하기 쉬운 크기 → 금지 문구 필수". ★B안의 `.pth` 도 같은 크기다 | — | `plan:659-672, 698, 828, 932` |
| **X13** | **증거 회수 채널은 화면 캡처뿐** — Windows 게스트 Desktop 쓰기가 UnauthorizedAccessException 으로 거부되고 Parallels `SharedProfile.UseDesktop=1` 에도 Mac↔Windows 파일 전달 양방향 실패(실측) | — | `plan:205-208` |
| **X14** | **'이벤트 로그에 없음'을 사건 부재의 근거로 쓰지 마라** — 반쪽 좌석은 agent_meta 부재로 데몬이 죽음을 감지조차 못 한다(agent.exited 이벤트 없음) | — | `plan:184-186, 174-183` |
| **X15** | **D4 는 claude 실패의 원인이 아니다** — claude 는 `ready_marker: "❯"` 가 있어 시간 폴백을 안 탄다. D4 는 marker 없는 에이전트(codex·grok)에서만 발현. **claude 실패 원인은 D2** | — | `plan:331-332` |
| **X16** | **리뷰어 지시서에서 '깨라/우회/공격' 표현 회피** — 2차 중 codex 안전 필터가 공격형 보안 요청으로 오분류해 응답 차단(중단 1회). 실제 작업은 '빌드 도구 정적 분석기의 회귀 테스트 품질 검토' | 운영 규약 | `plan:1177-1179` |
| **X17** | **§4 ACCEPT 7건 무손상** — 건드리면 회귀가 나는 자산: (a)2차 수정이 자기 축에서 작동 (b)builtins opaque 방어 성립 (c)`_path_aliases` 고정점 전파 자체는 정확 (d)**③ 이 안 도는 게 문제이지 ③ 자체는 무력하지 않다** (e)실물 팩 76개 rc=0·살아있는 D1 인스턴스 0건 (f)스텝 경계 판별 실패 시 exit 1 은 의도된 fail-closed (g)기각 3건에서 의도된 설계가 옳았음 | — | `round3:304-332` |
| **X18** | **P10 회귀 검체는 반드시 분석기 수정과 같은 커밋에** — 검체만 넣고 분석기를 안 고치면 스위트가 **영구 빨강** | — | `round3:371` |
| **X19** | **PATH 관련 변경 시 `install_cli_to_path` 를 함께 검토** — 인접 문서 `docs/plans/2026-06-29-cli-path-install.md` 는 보호대상①②와 다른 기능이지만 PATH 도메인을 공유한다 | — | `plan:13` |

---

## 8. 철회·기각 기록 (19건) — **재제안 방지**

| ID | 안 | 철회 사유 | 출처 |
|---|---|---|---|
| **W1** | **사망 감지를 `seat` 기반으로 전환** | `agent_meta` 는 *의미적* 판정, `seat` 는 *존재* 판정. 교체 시 **정밀도 하락**(자식 프로세스만 남으면 사망 놓침) + agent_seen 래치·401 차단·3회 서킷브레이커·poison 원장 **4겹 상실**. 치명 위험 ①폭주 ②무clear ③자가치유 전멸 직결. → 대안은 **교체가 아니라 추가**(`role.seat_vacant`, §6 Q9) | `plan:608, 933` |
| **W2** | **`cfg!(unix) \|\| env_injected` 제거·교체** | 그 줄은 플랫폼 차별이 아니라 **계정격리 계약**. 제거 시 Windows 에서 `CLAUDE_CONFIG_DIR` 없이 in-seat 기동 → **멀티계정 격리 붕괴**(치명 위험 ④). → 올바른 방향은 **F3**(§4 T26) | `plan:609, 933` |
| **W3** | ★★**`._pth` 에 팩 bin 경로 추가 / `.pth` 파일 배치** | **원 철회 사유 전문**: "팩 레인이 가변(`CYS_PACK_DIR`, 부서 팩)인데 정적 경로는 **한 레인에 고착** → **부서 격리 위반**". ★**오너가 재개봉했다**(2026-07-29, B안) — 근거: `.pth` 의 `import` 로 시작하는 줄은 **실행되는 코드**라 정적일 이유가 없다(실측 확인). 즉 철회 근거를 반박한 게 아니라 **철회 근거가 다루지 않은 경우를 여는 것**이다. ★단 구획8 실측으로 "부서 격리를 안 깬다"는 재개봉 논거도 **부분적으로만 참**임이 드러났다(§1 BR1 — env 레인 ≠ 스크립트 레인) | `plan:610` · 재개봉 `plan:1233-1261` · `round3:437-442, 489-493` · 한계 실측 구획8 |
| **W4** | **preflight 에 회귀 잠금 추가** | 방향은 옳으나 **부트 게이트**라 릴리스 중 온보딩 실패 위험 → 같은 목적을 **테스트**로(§6.2 = §2-A G4·G14) | `plan:611, 507` |
| **W5** | **빈 surface 수정안 3종** | 오너 결정으로 **종결**(범위 밖). 3종의 내용은 §7 표에 나열돼 있지 않다 | `plan:612` |
| **W6** | **빈 surface 잔존 건 자체** | 오너 결정으로 **종결**(오너·임시알바 좌석으로 확정). **재론하지 않는다**. ⚠§3.2/§3.5 의 '고아 좌석'과는 **다른 건**(§6 Q12) | `plan:10` |
| **W7** | **D안 — ▶CEO 버튼 완전 제거(레거시 회귀)** | **기각**, 근거 4종: ①버튼은 5개 좌석 중 1개만 담당(나머지 4명도 `cys boot`→launch-agent 로 동일 D2 경로) ②**부활(phoenix)이 죽는다**(`cys restore`·node-recover 동일 경로 → Windows 자동 부활 영구 불능 + 조용한 실패) ③레거시 경로 산물이 곧 **반쪽 좌석**(claim-role 은 agent_meta 미기록) ④2026-07-15 실사고의 결정론 처방. ★단 근거④는 role-bootstrap.sh 훅 도입으로 **4개 중 가장 약하다**고 정직히 기록됨 | `plan:561-576, 933` |
| **W8** | **B안(Windows 에서만 사람 위임) · C안(버튼을 설정으로 끔, 기본 켬)** — §6.7 선택지 | B=플랫폼 분기 증식으로 **미채택**. C=선택지만 늘고 결함 잔존으로 **미채택**. → **A안 채택**(버튼 유지 + 자동 시도 → 실패 시 사람 위임 + D3·D5·D6 수리) | `plan:553-563, 578-580` |
| **W9** | **D3 3안 — 익명 경로 위임**(launch-agent 가 close 만 pane 밖 프로세스로 위임) | **취약·비권장**으로 평가(명시적 '철회'는 아님 — 재제안 방지 기록으로 남김) | `plan:301` |
| **W10** | **A안 — 4차 분석기 추상해석 재설계**(값 격자·바인딩 환경·호출그래프 도달성, ⊤=무조건 금지) | 큰 작업이고 **`.pth` 가 채택되면 '없어도 되는 층'에 투자한 것이 된다**. → **오너가 B안을 택해 미채택**. 남는 정적층 BLOCK 은 '수용된 잔여 리스크'로 명기해야 한다(§2-B) | `plan:1256, 1227-1229` |
| **W11** | **C안 — 현 수준 동결만**(4차도 `.pth` 도 없음) | 정적층 BLOCK 이 남는 대가는 같으나 **B안이 C안을 포함하고 `.pth` 를 더한다** → 미채택 | `plan:1258` |
| **W12** | **`$env:LOCALAPPDATA` 재귀 검색 범위 축소**(T6 설치 경로 발견) | **의도적 미반영** — T3·T4·T5 가 쓰는 **검증된 형태**와 일부러 일치시켰다. T6 만 바꾸면 설치 경로 발견 로직이 갈린다. 리뷰어도 '완료는 된다'고 판정 | `plan:1073` · `r2-ci-gemini.md:41-42` |
| **W13** | **`sys.path.pop(0)` 과탐 수정** | **의도적 미반영** + 계약 명문화("가드를 건 뒤 형제 import 전까지 `sys.path` 를 건드리지 마라"). 근거 3: ①실물 팩에 해당 형태 0건 ②과탐은 CI 에서 시끄럽게 드러나 즉시 고쳐지지만 **미검출은 Windows 에서 조용히 산다** ③'무해함을 증명하는 분석'은 경계가 없다. ⚠단 부록A(4)(§2-B FZ19)가 근거②를 반증했다 | `plan:1143-1149` · `round2-codex.md:179-187` · 반증 `round3:507-511` |
| **W14** | **`spec_from_file_location` 형태를 검사 대상에 편입** | 명시 파일 경로 로딩은 sys.path 등재가 불필요해 **결함을 재현하지 않는다** → 조치 없음(공격 실패 판정). 현재도 SCAN=OMIT 실측 확인 | `r1-adversarial-codex.md:217-225` |
| **W15** | **D5-3 — ③ timeout 예산(960s) > T6 스텝(300s) 이라 진단 경로 도달 불가** | **기각**. 산술은 재현되나 표제 주장이 실행으로 **반증**됨 — 매다는 검체로 정확히 120.0s 에 TimeoutExpired 분기 발동. 1종 행=120.2s·2종 행=240.2s 로 예산 내 완주, 3종 이상이어도 첫 행 하나로 ③ FAIL→exit 1, 스텝이 먼저 죽어도 동일 fail-closed. **※T6-3(§2-C N5)은 별건으로 살아 있음** | `round3:298, 535-541` |
| **W16** | **D5-4 — 자식 프로세스 격리가 env·HOME·cwd 엔 미성립** | **기각**. 소스 주장은 '`sys.path`·`sys.modules` 그 부류'로 한정돼 있고 그건 참. env 상속은 격리 실패가 아니라 **의도된 충실도**(팩 스크립트가 `CYS_PACK_DIR` 등을 정당히 읽는다). 공유 cwd 오염 실측 0건, 판정 영향 0 | `round3:299, 542-547` |
| **W17** | **T6-2 — 인코딩 크래시가 'D1 재발'로 오귀인된다** | **기각**. 근거로 든 저장소 선례 2건이 **오독**(`javis_bootstrap.py:50-51` 주석은 'PYTHONUTF8 이 부족하다'가 아니라 'export 가 cys-dept 경로에만 있다'인데 T6 는 스텝 `env:` 로 명시 설정). 재현이 CI 조건 아님(`PYTHONIOENCODING=cp1252` 강제). 방향도 fail-closed. ⚠**핵심 전제(`._pth` 가 PYTHONUTF8 을 무효화하는가)는 양쪽 다 미검증** — B안이 `._pth` 를 다루므로 §1 BR7 에서 함께 실측 | `round3:300, 548-551, 346` |
| **W18** | **회귀 잠금을 부트 게이트로** | W4 의 짝 — **테스트 레인에만** 둔다는 설계 결정(릴리스 중 부팅 실패 위험 0) | `plan:507, 611` |
| **W19** | **`master.deadman` 경보를 새로 만든다** | **이미 존재한다** — `governance.rs:537-557 check_master_deadman`(master surface 무출력 ≥ `CYS_MASTER_DEADMAN_SECS`, 기본 900초) → UI 토스트 실측 확인. 반쪽 좌석 문제의 보완 신호로 이미 있으므로 새로 만들 필요 없다(철회안 P5 의 대체 후보) | `plan:199-204` |

---

## 부록 — 오너가 지금 답해야 하는 11건 (결정 대기)

| # | 결정 | 막고 있는 것 | 근거 |
|---|---|---|---|
| 1 | **C1 커밋 승인** — 오늘 상태(tracked 12 + untracked 7)를 커밋할 것인가 | 모든 후속(§4 티켓 전부가 "티켓A 종결 후 착수") | `plan:919, 1281, 1257` |
| 2 | **BR3 `.pth` 배치 위치** — prefix vs Lib/site-packages | §1 구현 전부 | 구획8 실측 |
| 3 | **BR4 `.pth` 파일명** | BG3 검증 | `windows-build.yml:63-67` |
| 4 | **BR6 argv 기준 대안 채택 여부** — 채택 시 L1 교차오염 소멸 | §1 설계 확정 | 구획8 |
| 5 | **BI3 `prep-win-runtime.sh` 추출 여부** | 복제 지점 관리 | `release.yml:300-305` |
| 6 | **FZ25 조건부 재정의 과탐 수용 여부** — 정상 코드가 FAIL 나는 것을 의도된 대가로 볼 것인가 | 동결 목록 확정 | 구획6 실측 |
| 7 | **T2 D6 ①/② 택1** (원문은 ②를 정도로 지목) | T-D6 착수 | `plan:361-368` |
| 8 | **T4 D3 수리안 3중 택1** (1안 nonce 권장) | T-D3 착수 → R4 성립 | `plan:299-302` |
| 9 | **Q8 회귀 테스트 거처** — 팩 python vs Rust 소스 grep. B안이면 검증 대상 자체가 바뀐다 | 회귀 장치 설계(§17 원칙4) | `plan:780` |
| 10 | **Q9 `role.seat_vacant` 를 이번 판에 넣을 것인가** | §11-7 체크와 충돌 여부 | `plan:608, 781` |
| 11 | **Q10 F1 skip 대상 9개 분류 기준** | F1(§4 T24) 착수 | `plan:782` |

> **커밋 전 오너에게 반드시 고지해야 할 것 2건**(문서 미기재 · 구조상 도출):
> 1. **FZ26** — 3차 반영 2건(③ 실측 전환 · escape opaque)은 **적대 검증을 한 번도 받지 않았다**. 가드 동결 = 이 2건이 미검증 상태로 고정된다.
> 2. **§2-C N7** — T6 스텝은 **실제 Windows 러너에서 0회 실행**됐다. mac 에 `pwsh` 가 없어 구문검사조차 못 했다. 첫 실행 로그를 보기 전까지 T6 는 미검증이다.


---

# 부록 — 누락 감사

## 감사 방법
`docs/plans/2026-07-29-win-two-defects-plan.md` 1286줄 전문 + 리뷰 보고서 5종(r1 230줄·r2 109줄·round2-codex 208줄·round2-gemini 116줄·round3 551줄) + 런북 157줄을 정독하고, 구현 동사 grep(211줄 후보) · 체크박스/이월/오너판단/미정 전수 검색 · `git diff` 전량(284+/-13, staged 922줄) · 실측(`main.rs:2153 spawn_orchestra_boot`, `test_import_guard.py` docstring·③ 구현, `skills/*/scripts` 실재)으로 대조했다. **작업트리는 수정하지 않았다.**

## 리스트의 커버리지 — 대체로 매우 높다
- **3차 보고서 확증 35건 전수 매핑 확인**: BLOCK 12건(D1-F1~F4·D2-1·D2-2·D3-1·D4-3·D5-1·D6-1·F7-1·T6-1) · REVISE 19건 · NIT 4건 · 기각 3건(D5-3→W15·D5-4→W16·T6-2→W17)이 모두 FZ/N/W 항목으로 존재한다. 누락 0.
- **1·2차 보고서**: BLOCK/REVISE 전건이 G11~G13 또는 FZ14/FZ23/W12~W14 로 대응된다. round2-codex 의 `missing` 3필드도 FZ16·W10·G12 로 흡수됐다.
- **`git diff` 전량 설명 가능**: 워크플로 4종(G6·G7·G8·G9·G10) · 팩 7파일 +12줄(G1·G2) · 신규 스위트 922줄 A 상태(G4·G18). 리스트가 설명 못 하는 변경 **0건**.
- 숫자 실측도 일치: tracked 12(M11+A1) · untracked 7 · 86/86 · 46/46.

## 발견한 누락 15건의 성격
새로 발명한 것은 없다. 전부 **원문에 문장으로 실재하는데 리스트가 요약하면서 절반을 자른 것**이다. 세 패턴으로 갈린다.

1. **처방의 뒷절 절단**(6건: #1·#10·#11·#14·#15 그리고 #2) — 3차 부록A 의 '닫는 방법 (a)(b)(c)(d)' 중 (a)만 옮긴 사례가 반복된다. 특히 **#1(.pth 재평가 축 3종)과 #2(sitecustomize)** 는 ★B안 채택 판단 자체를 좌우하므로 BI 계열 착수 전에 §1-A 로 들어가야 한다.
2. **표 한 행 누락**(3건: #3·#4·#12) — 3차 §5 위험표 8행 중 'Windows 경로 의미론'만, §6 'BLOCK 해제 기준' 4종 전체가, T6-2 기각의 잔여 NIT 가 빠졌다. **#3은 동결 대상이 아니다** — `.pth` 멱등 가드(BI4)와 가드 7파일의 중복검사가 같은 문자열 비교라 B안 구현에 직결된다. **#4는 커밋 전 오너 고지 항목**이 하나 더 있다는 뜻이다(현 부록은 FZ26·N7 2건만 고지).
3. **짝 문서·산출물 자체의 정정**(3건: #6·#7·#13) — 리스트의 문서 정정 항목(C8)이 기획서 내부에만 한정돼, 커밋 후보인 **런북과 스위트 docstring** 의 낡은/반증된 문장이 감사망 밖에 있었다. #6(런북 Desktop 출력 vs X13)은 T20 착수 즉시 막히는 **선행 수리**다.

추가로 **#5(spawn_orchestra_boot)** 는 §4 티켓의 원자 분해 실패다 — start_master 만 고치면 5좌석 중 1좌석만 구제되고 나머지 4명은 D2 경로를 그대로 탄다(plan:567 이 명시).

## 우선순위 권고
착수 순서로는 **#4(BLOCK 해제 기준 실측·고지) → #1·#2(B안 설계 확정 전 선행) → #3(BI4 선행) → #5 → #6** 순이 맞다. #4 는 커밋 승인(C1) 자리에서 오너가 알아야 할 정보이므로 문서 작업이 아니라 **게이트**로 다뤄야 한다.

## ★`.pth` 재평가 축 3종이 통째로 빠졌다 — ①이 런타임을 쓰는 **모든 프로세스**(pip 설치 CLI 포함)에 pack bin 이 sys.path 에 들어가는 부작용 ②`append` 유지 시 shadowing 위험 ③**팩 미설치 상태**에서의 동작
- 출처: docs/plans/review-2026-07-29-round3-workflow.md:493 (닫는 방법 (b)) · 근거 :442, :489-491
- 왜 빠졌나: 리스트 §1-A 는 BR1~BR8 로 검토 8건을 세웠지만 BR8(pip/uv site-packages 공유)만 이 축 중 일부를 담았다. 원문은 '재평가 축은 **새 것이어야 한다**'며 세 축을 명시했는데, 리스트는 채택 전 반드시 답해야 할 이 세 질문 중 둘을 누락했다. 특히 ②는 §7 X4(append 교리)·G2 와 정면으로 얽히고, ③은 `~/.cys/pack` 부재 기계(신규 설치 직후·부서 팩 미배포)에서 `.pth` 가 존재하지 않는 경로를 append 하게 되는 실동작 문제라 BR3(배치 위치) 결정과 독립이다.
- 편입 위치: §1-A 검토 단계에 BR9(전 프로세스 노출 부작용)·BR10(append shadowing 재평가)·BR11(팩 미설치 상태 동작) 3행 추가 — 전부 '결정필요' 등급, BI 계열 착수 전 선행

## ★`sitecustomize.py` 대안이 리스트 전체에 0회 등장 — 원문은 `.pth` 와 **병기된 두 후보**로 제시했다
- 출처: docs/plans/review-2026-07-29-round3-workflow.md:437, :489, :493(a) ("`.pth`/`sitecustomize` 경로가 살아 있다" · "`.pth`/`sitecustomize` 를 정적 경로 형태가 아니라 실행 코드 형태로 재평가하라")
- 왜 빠졌나: 오너 발언과 기획서 §18.11.4(plan:1233-1247)가 `.pth` 만 언급해 리스트가 그 어휘를 그대로 승계했다. 그러나 재개봉 근거를 만든 3차 보고서 원문은 두 메커니즘을 항상 병기했고, `import site` 가 살아 있다는 동일 전제 위에 둘 다 성립한다. `sitecustomize` 는 파일 1개·정상 파이썬 문법이라 BI4(멱등 가드)·BI5(예외 안전화)의 처리가 `.pth` 한 줄 제약(1줄 강제·예외 시 stderr 오염)보다 쉬울 수 있어, 배치·파일명 결정(BR3·BR4) 이전에 비교돼야 한다.
- 편입 위치: §1-A 에 BR12 '`.pth` vs `sitecustomize.py` 메커니즘 택1' 추가(결정필요) — BR3·BR4 의 선행조건으로 배치

## ★Windows 경로 의미론이 전건 미검증 — `if _S not in sys.path` 문자열 멤버십이 대소문자 무시·백슬래시·8.3 단축명 하에서 어떻게 되는지 아무도 안 봤다
- 출처: docs/plans/review-2026-07-29-round3-workflow.md:342 (§5 남은 미검증 영역 표, 위험 '중간')
- 왜 빠졌나: 리스트는 3차 §5 위험표 8행 중 7행(T6 0회 실행→N7, 음성대조→N7, 61개 파일→FZ11, skills 4건→FZ13, ③미도달 7파일→FZ12, 인코딩→W17, 로그절단→N5)을 흡수했으나 이 행만 흘렸다. 동결 대상이 아니다 — **가드 7파일의 중복 가드 조건과 §1 BI4(`.pth` 멱등 가드 `if p not in sys.path`)가 정확히 같은 문자열 비교**라, 이 축이 깨지면 가드가 매 기동마다 경로를 중복 append 하거나(BI4 무력화) 반대로 정상 경로를 이미 있다고 오판한다. 게다가 ③ 프로브의 자식 프로세스도 `._pth` 를 상속해 mac 과 sys.path 초기값이 다르다는 지적이 함께 있는데 이것도 리스트에 없다.
- 편입 위치: §1-C 에 BG8(Windows 경로 정규화 실측 — BI4 의 선행) 추가 + §2-B 에 '가드 중복검사의 Windows 의미론 미검증' 1행(동결이라도 리스크 기록)

## ★3차 총판정 **BLOCK 의 해제 기준 4종**이 리스트에 없다 — 동결 = 이 4개가 미충족인 채로 커밋된다
- 출처: docs/plans/review-2026-07-29-round3-workflow.md:373-378 (§6 'BLOCK 해제 기준' 1~4)
- 왜 빠졌나: 리스트는 3차의 확증 35건·기각 3건·처방 P1~P10 은 전수 흡수했으나, 보고서가 명시한 **판정 해제 조건**은 항목화하지 않았다. 이건 단순 누락이 아니라 커밋 게이트다 — 4기준 중 ②(실물 3파일 한 토큰 오타 시 exit≠0)는 3차 반영분(③ sys.modules 실측)으로 닫혔을 가능성이 있고 ③(pack-release 배송 게이트 `if: false` 검출)은 N1 미착수라 명백히 미충족, ①④는 미측정이다. 부록의 '커밋 전 오너 고지 2건'(FZ26·N7)과 같은 성격인데 이 세 번째가 빠졌다.
- 편입 위치: 부록 '커밋 전 오너 고지'에 3번째 항목 추가 + §5 검증 절에 V14 '3차 BLOCK 해제 기준 4종 현재 충족 여부 실측·기록'

## ★T-D6 의 `--cwd` 명시 대상에서 **`spawn_orchestra_boot`** 가 빠졌다 (원문은 `start_master`/`spawn_orchestra_boot` 둘을 나란히 지목)
- 출처: docs/plans/2026-07-29-win-two-defects-plan.md:367 ("①`start_master`/`spawn_orchestra_boot`이 `--cwd`를 명시하도록 할지") · 실측 src-tauri/src/main.rs:2153, :2138, :2277
- 왜 빠졌나: 리스트 T1 이 대상을 `state.rs` + `main.rs:2124-2130`(=start_master)로만 적었다. 그런데 D안 기각 근거①(plan:567)이 명시하듯 **나머지 4좌석(CSO·워커·리뷰어2)은 `cys boot`→launch-agent 경로**로 뜨고, 그 `cys boot` 를 호출하는 곳이 `spawn_orchestra_boot`(main.rs:2153, 호출부 :2138 본부 · :2277 부서장)다. start_master 만 고치면 master 1좌석만 홈에서 뜨고 나머지 4좌석은 그대로 설치폴더 → D2 발현이 유지된다. T2(①/② 택1)의 ① 차선책을 택할 경우 특히 치명.
- 편입 위치: §4-A T1 의 '대상' 칸에 `src-tauri/src/main.rs:2153 spawn_orchestra_boot`(+ 호출부 :2138 · :2277) 추가, 또는 T1b 로 분리

## 증거 수집 런북 A-0~A-7 이 **Desktop 파일 출력**에 의존하는데, 그 채널은 실측으로 실패가 확정됐다(X13) — 런북 개정 필요
- 출처: docs/plans/2026-07-29-win-evidence-runbook.md:24, :31, :37, :45, :75, :84 (`> $env:USERPROFILE\Desktop\...`) vs docs/plans/2026-07-29-win-two-defects-plan.md:205-208 ("Desktop 쓰기가 UnauthorizedAccessException 으로 거부 … 증거 회수 채널은 화면 캡처뿐임을 전제로 런북을 **설계할 것**")
- 왜 빠졌나: 리스트는 X13 을 '제약'으로만 계상하고(§7), 런북은 C3 에서 '커밋 여부' 대상으로만 다뤘다. 그래서 **제약이 이미 위반된 산출물이 존재한다**는 사실이 어디에도 안 남았다. plan:208 은 명령형('설계할 것')이며 T20(§13 증거수집)이 실행되는 순간 6개 파일 저장이 전부 실패해 CSO/오너가 A-0 에서 막힌다. T20 의 선행 수리다.
- 편입 위치: §4-C T20 앞에 T20a '런북 A-0~A-7 의 출력 채널을 화면 캡처 기반으로 재작성(Desktop 리다이렉션 제거)' 추가

## 런북 §D-1 이 아직 '오너: ▶CEO 버튼으로 master 복구'를 지시한다 — §6.1 이 Windows 무효로 정정된 절차다
- 출처: docs/plans/2026-07-29-win-evidence-runbook.md:143-145 vs docs/plans/2026-07-29-win-two-defects-plan.md:481-488 ("Windows ❌ 무효 — ▶CEO 2회 시도 모두 실패 실측")
- 왜 빠졌나: 리스트 T18 은 기획서 §6.1 의 ⚠표기 상태 갱신만 다루고, C8 의 '낡은 수치·표 정리' 8항목도 전부 기획서 내부에 한정된다. 짝 문서인 런북에 같은 낡은 처방이 남아 있고, 그 문서가 지금 커밋 후보(C3)라 **틀린 지시가 그대로 저장소에 들어간다**. T22(§6.6 수동 우회)가 대체 절차인데 런북은 그걸 모른다.
- 편입 위치: §3 C8 항목에 ⑨ '런북 §D-1 을 §6.6 수동 우회로 교체(정정 이력 보존)' 추가

## D2 설계 시 함께 검토하라고 명시된 **`\r\n` 대안**이 T5~T8 어디에도 없다
- 출처: docs/plans/2026-07-29-win-two-defects-plan.md:250 ("단발 전송·전송 후 프롬프트 소멸 확인·재전송 상한·**`\r\n` 대안**·그리고 애초에 신뢰창이 뜨지 않게 하는 선행 조치")
- 왜 빠졌나: plan:250 의 5개 검토 항목 중 4개는 리스트에 매핑됐다(단발→T5, 소멸확인→T6, 재전송 상한→T5, 선행조치→T12). `\r\n` 대안만 빠졌다. 이건 Q1(M1 규명)과 다른 층이다 — Q1 은 '왜 `\r` 이 취소로 처리되나'(원인)이고 이건 '다른 바이트열로 보내면 되나'(우회 실험)라, M1 이 끝내 미규명이어도 R1~R4 착수를 가능하게 하는 유일한 실증 경로일 수 있다.
- 편입 위치: §6 Q1 아래 Q1a '`\r\n`·대체 키 시퀀스 전송 실험' 또는 §4-A T5 의 선행 실험으로 추가

## §15 R3 — '무코드 처방은 사람 습관 의존 → **다음 판에 재검토**'가 열린 질문 12건에 없다
- 출처: docs/plans/2026-07-29-win-two-defects-plan.md:775 ("결정론화는 부트스트랩 경로 변경이 필요(현 시점 미채택). **다음 판에 재검토**")
- 왜 빠졌나: 리스트는 §15 리스크 5건 중 R1→Q7, R2→V8, R4→T24, R5→T25 로 4건을 흡수했는데 R3 만 흘렸다. 명시적 이월 항목('다음 판에 재검토')이고, T22(수동 우회로 만든 반쪽 좌석)·T17(임시 방편 문서 명시)와 같은 뿌리다 — 오늘 오너가 쓸 우회로가 습관 의존이라 잊으면 반쪽 좌석이 재발한다는 것이 R3 의 요지이므로, T22 를 실행하는 순간 이 리스크가 활성화된다.
- 편입 위치: §6 열린 질문에 Q13 '무코드 처방의 결정론화(부트스트랩 경로 변경) 이번 판 채택 여부' 추가

## 부록A(5) 닫는 방법 (a)·(c) 누락 — T6 음성 대조를 **훅과 동일한 해소 경로**로 한 번 더 돌려 같은 실행파일인지 확인(다르면 fail-closed) / 불가하면 계약 문서에 '번들 인터프리터일 때만 유효' 명시 + 비율 측정
- 출처: docs/plans/review-2026-07-29-round3-workflow.md:517 (닫는 방법 (a)(b)(c)) · 지적 본문 :513-515
- 왜 빠졌나: 리스트 BD3 은 (b)(훅의 `CYS_PY` 를 번들 절대경로 우선으로 고정)만 담았다. (a)는 게이트 자신의 전제 검증(하드코딩 `$dir\runtime\python\python3.exe` vs `command -v python3` 일치)이라 **BD3 의 결정과 무관하게 T6 에 넣을 수 있는 스텝**이고, (c)는 고정이 불가능할 때의 대체 조치다. B안 채택 시 `.pth` 는 번들 인터프리터에만 실리므로 '생산이 다른 파이썬을 쓸 수 있다'는 이 축의 중요도가 오히려 올라간다.
- 편입 위치: §1-C 에 BG9(T6 인터프리터 해소 일치 검증 스텝) + §1-D BD3 에 (c) 계약 문서 명시·비율 측정 분기 추가

## 부록A(2) 닫는 방법 (b)·(c) 누락 — 각 스크립트에 **부작용 없는 생산 진입점 규약**(`--help`/no-op) / ③ 대상 선정을 `toplevel` 이 아니라 **'생산에서 어떻게 기동되는가'(훅 등록 표)** 기준으로
- 출처: docs/plans/review-2026-07-29-round3-workflow.md:499 (닫는 방법 (a)(b)(c)) · 근거 :495-497
- 왜 빠졌나: 리스트 FZ17 은 (a)(`[sys.executable,'-I',path]` 자식 프로세스 교체)만 담고 '한 플래그 작업'으로 요약했다. 그런데 (a)를 실제로 하려면 `__main__` 이 도는 순간 부작용이 있는 스크립트를 어떻게 부를지가 함께 풀려야 하므로 (b)는 (a)의 전제다. (c)는 FZ11(P8a 배송 표면 전체 확장)과 **다른 기준**이다 — P8a 는 '어느 파일을', (c)는 '어떤 진입형으로'라 파일 선정을 완벽히 고쳐도 함수내 12건(60%)은 남는다고 원문이 명시했다.
- 편입 위치: §2-B FZ17 을 FZ17a(진입형 교체)·FZ17b(무부작용 진입점 규약)·FZ17c(③ 대상 기준을 훅 등록 표로)로 분해

## T6-2 기각의 **잔여 NIT — `reconfigure` 한 줄 추가**가 리스트에 없다
- 출처: docs/plans/review-2026-07-29-round3-workflow.md:300 ("※ `reconfigure` 한 줄 추가는 값싼 개선으로 NIT 잔여") · :346 (§5 위험표 'PowerShell 인코딩(T6-2 잔여) — 기각됐으나 reconfigure 미적용')
- 왜 빠졌나: 리스트 W17 이 T6-2 를 '기각' 한 줄로 처리하면서 보고서가 같은 문단·같은 표에서 두 번 남긴 잔여 조치를 함께 지웠다. 기각된 것은 '인코딩 크래시가 D1 재발로 오귀인된다'는 표제이지 개선 자체가 아니다. §1 BR7 에서 `._pth` × PYTHONUTF8 상호작용을 어차피 실측하기로 했으므로 그 작업과 같은 자리다.
- 편입 위치: §2-C 에 N11 'T6/스위트에 `sys.stdout.reconfigure` 1줄(값싼 이중 방어)' 추가 — W17 의 잔여로 표기

## 회귀 스위트 **docstring 자체의 정정 3건** — ①이력2 라벨 오기('2차'→1차) ②3차 반영 이력 미기재 ③"가드 식 예외는 ③ 이 잡는다" 자기 예제가 D4-3 로 반증됨
- 출처: cysjavis-pack/bin/tests/test_import_guard.py:29(이력2 라벨) · :50(이력3) · :79-82(한계 절) vs docs/plans/review-2026-07-29-round3-workflow.md:114, :222-227 ("docstring 이 '이건 ③ 이 잡는다'고 자기 예제로 못박은 `sys.path.append((1/0,_S)[1])` 조차 형제 import 를 함수 안에 두면 … exit 0")
- 왜 빠졌나: 리스트 C8 의 '문서 정정'은 기획서 8항목에 한정되고, FZ19 는 docstring 69-70행(과탐 논증)만 지목했다. 스위트 파일은 커밋 후보(A 스테이징)이고 다음 사람이 유일하게 읽을 계약 문서인데, ③은 3차가 **실증으로 반증한 문장**이 그대로 남아 '이 층은 저 층이 덮는다'는 거짓 안심을 준다. ①②는 이력 번호가 어긋나 1차 반영분이 사라진 것처럼 읽힌다(이력2 가 r1 인데 '2차'로 적힘, 3차 반영분은 함수 docstring :333/:614 에만 있고 모듈 이력엔 없음).
- 편입 위치: §3 C8 에 ⑩ '스위트 docstring 정정 3종(이력 라벨·3차 이력 신설·③ 자기예제 반증 반영)' 추가

## 부록A(4) 닫는 방법 (d) 누락 — 게이트 실패 메시지에 **'우회하지 말고 이렇게 고쳐라'의 정답 형태**를 박아 넣기
- 출처: docs/plans/review-2026-07-29-round3-workflow.md:511 (닫는 방법 (d))
- 왜 빠졌나: 리스트 FZ19 는 (a)(과탐 안전 논증 폐기)·(b)(억제 채널 신설)만 담았고 (c)는 FZ9 로, (d)는 어디에도 안 갔다. (d)는 분석기 수정이 아니라 **메시지 문자열 작업**이라 동결(가드 현 수준 유지)과 충돌하지 않는 유일한 부록A(4) 조치다 — 실측이 보인 '소음의 가장 싼 해소법이 미검출 생산'을 막는 비용 최저 층이다.
- 편입 위치: §2-C 에 N12 '게이트 실패 메시지에 정답 형태 명시(동결과 무충돌)' 추가

## D6-3 처방의 조건 절 누락 — 검사 표면을 `skills/*/scripts` 로 확장할 때 **`check_timesfm.py:166` 조회형 예외**를 함께 넣어야 한다
- 출처: docs/plans/review-2026-07-29-round3-workflow.md:271 (REVISE 표 D6-3 처방 칸: "표면 확장 + `check_timesfm.py:166` 조회형 예외") · 실측 cysjavis-pack/skills/timesfm-forecasting/scripts/check_timesfm.py
- 왜 빠졌나: 리스트 FZ13 이 처방 칸의 앞 절(표면 확장)만 옮기고 뒤 절(예외)을 잘랐다. 원문은 두 절이 한 처방이며, 예외 없이 표면만 넓히면 FZ9(D4-1/P7 조회형 vs 부작용형 구분)가 미착수인 현 상태에서 `check_timesfm.py` 가 즉시 거짓 FAIL 한다 — FZ13 이 '현재 넷 다 가드 보유 · 회귀 보호만 0'이라 무해하다고 적은 판단이 이 예외 없이는 성립하지 않는다.
- 편입 위치: §2-B FZ13 의 '안 하는 일' 칸에 `+ check_timesfm.py:166 조회형 예외` 병기, 선행조건에 FZ9 표기